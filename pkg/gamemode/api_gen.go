// Code generated by tools/codegen - DO NOT EDIT
package gamemode

import (
	"context"
	"errors"
	omp "github.com/ompgo-dev/ompgo/pkg/omp"
	runtime "github.com/ompgo-dev/ompgo/pkg/runtime"
)

// Common API errors
var (
	ErrNotImplemented         = errors.New("gamemode: feature not yet implemented")
	ErrInvalidPlayer          = errors.New("gamemode: invalid player handle")
	ErrInvalidVehicle         = errors.New("gamemode: invalid vehicle handle")
	ErrInvalidActor           = errors.New("gamemode: invalid actor handle")
	ErrInvalidObject          = errors.New("gamemode: invalid object handle")
	ErrInvalidPlayerObject    = errors.New("gamemode: invalid playerobject handle")
	ErrInvalidPickup          = errors.New("gamemode: invalid pickup handle")
	ErrInvalidTextDraw        = errors.New("gamemode: invalid textdraw handle")
	ErrInvalidPlayerTextDraw  = errors.New("gamemode: invalid playertextdraw handle")
	ErrInvalidTextLabel       = errors.New("gamemode: invalid textlabel handle")
	ErrInvalidPlayerTextLabel = errors.New("gamemode: invalid playertextlabel handle")
	ErrInvalidGangZone        = errors.New("gamemode: invalid gangzone handle")
	ErrInvalidMenu            = errors.New("gamemode: invalid menu handle")
	ErrInvalidCheckpoint      = errors.New("gamemode: invalid checkpoint handle")
	ErrInvalidClass           = errors.New("gamemode: invalid class handle")
	ErrInvalidNPC             = errors.New("gamemode: invalid npc handle")
	ErrNotInitialised         = errors.New("gamemode: API not Initialised")
)

// API provides access to server functions for use in gamemodes.
type API struct {
	capi *runtime.CAPI
}

// apiInstance is the global API instance
var apiInstance *API

// InitAPI initializes the API with a CAPI instance
func InitAPI(capi *runtime.CAPI) {
	if capi == nil || !capi.IsInitialised() {
		panic("gamemode: CAPI not Initialised")
	}
	apiInstance = &API{capi: capi}
}

// GetAPI returns the gamemode API instance.
func GetAPI() *API {
	return apiInstance
}

// Initialize returns the API instance or panics if it is not ready.
func Initialize() *API {
	if apiInstance == nil {
		panic("gamemode: API not Initialised")
	}
	return apiInstance
}

func init() {
	runtime.RegisterAPIInit(func(ctx context.Context, capi *runtime.CAPI) error {
		_ = ctx
		InitAPI(capi)
		return nil
	})
}

// IsInitialised returns whether the API has been properly Initialised
func (a *API) IsInitialised() bool {
	return a != nil && a.capi != nil && a.capi.IsInitialised()
}

// Log writes a message to the open.mp server console.
func (a *API) Log(message string) error {
	if !a.IsInitialised() {
		return ErrNotInitialised
	}
	if !a.capi.Core_Log(message) {
		return errors.New("gamemode: failed to log message")
	}
	return nil
}

// ActorFromID wraps CAPI Actor_FromID
func (a *API) ActorFromID(actorid int32) (*omp.Actor, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Actor_FromID(actorid)
	return omp.NewActor(res), nil
}

// Add wraps CAPI Class_Add
func (a *API) Add(team uint8, skin int32, x float32, y float32, z float32, angle float32, weapon1 uint8, ammo1 uint32, weapon2 uint8, ammo2 uint32, weapon3 uint8, ammo3 uint32, id *int32) (*omp.Class, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Class_Add(team, skin, x, y, z, angle, weapon1, ammo1, weapon2, ammo2, weapon3, ammo3, id)
	return omp.NewClass(res), nil
}

// AddComponent wraps CAPI Vehicle_AddComponent
func (a *API) AddComponent(vehicle *omp.Vehicle, componentid int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_AddComponent(vehicle.Handle(), componentid)
	return res, nil
}

// AddItem wraps CAPI Menu_AddItem
func (a *API) AddItem(menu *omp.Menu, column uint8, text string) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return 0, ErrInvalidMenu
	}
	res := a.capi.Menu_AddItem(menu.Handle(), column, text)
	return res, nil
}

// AddPickupStatic wraps CAPI Pickup_AddStatic
func (a *API) AddPickupStatic(model int32, typ int32, x float32, y float32, z float32, virtualWorld int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Pickup_AddStatic(model, typ, x, y, z, virtualWorld)
	return res, nil
}

// AddRule wraps CAPI Core_AddRule
func (a *API) AddRule(name string, value string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_AddRule(name, value)
	return res, nil
}

// AddStaticEx wraps CAPI Vehicle_AddStaticEx
func (a *API) AddStaticEx(modelid int32, x float32, y float32, z float32, angle float32, color1 int32, color2 int32, respawnDelay int32, addSiren bool, id *int32) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Vehicle_AddStaticEx(modelid, x, y, z, angle, color1, color2, respawnDelay, addSiren, id)
	return omp.NewVehicle(res), nil
}

// AddVehicleStatic wraps CAPI Vehicle_AddStatic
func (a *API) AddVehicleStatic(modelid int32, x float32, y float32, z float32, angle float32, color1 int32, color2 int32, id *int32) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Vehicle_AddStatic(modelid, x, y, z, angle, color1, color2, id)
	return omp.NewVehicle(res), nil
}

// AllowAdminTeleport wraps CAPI Core_AllowAdminTeleport
func (a *API) AllowAdminTeleport(allow bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_AllowAdminTeleport(allow)
	return res, nil
}

// AllowInteriorWeapons wraps CAPI Core_AllowInteriorWeapons
func (a *API) AllowInteriorWeapons(allow bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_AllowInteriorWeapons(allow)
	return res, nil
}

// AllowNickNameCharacter wraps CAPI Core_AllowNickNameCharacter
func (a *API) AllowNickNameCharacter(character int32, allow bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_AllowNickNameCharacter(character, allow)
	return res, nil
}

// AllowTeleport wraps CAPI Player_AllowTeleport
func (a *API) AllowTeleport(player *omp.Player, allow bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_AllowTeleport(player.Handle(), allow)
	return res, nil
}

// AllowWeapons wraps CAPI Player_AllowWeapons
func (a *API) AllowWeapons(player *omp.Player, allow bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_AllowWeapons(player.Handle(), allow)
	return res, nil
}

// ApplyActorAnimation wraps CAPI Actor_ApplyAnimation
func (a *API) ApplyActorAnimation(actor *omp.Actor, name string, library string, delta float32, loop bool, lockX bool, lockY bool, freeze bool, time int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_ApplyAnimation(actor.Handle(), name, library, delta, loop, lockX, lockY, freeze, time)
	return res, nil
}

// ApplyPlayerAnimation wraps CAPI Player_ApplyAnimation
func (a *API) ApplyPlayerAnimation(player *omp.Player, animlib string, animname string, delta float32, loop bool, lockX bool, lockY bool, freeze bool, time uint32, sync int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ApplyAnimation(player.Handle(), animlib, animname, delta, loop, lockX, lockY, freeze, time, sync)
	return res, nil
}

// AreAllAnimationsEnabled wraps CAPI Core_AreAllAnimationsEnabled
func (a *API) AreAllAnimationsEnabled() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_AreAllAnimationsEnabled()
	return res, nil
}

// AreInteriorWeaponsAllowed wraps CAPI Core_AreInteriorWeaponsAllowed
func (a *API) AreInteriorWeaponsAllowed() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_AreInteriorWeaponsAllowed()
	return res, nil
}

// AreWeaponsAllowed wraps CAPI Player_AreWeaponsAllowed
func (a *API) AreWeaponsAllowed(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_AreWeaponsAllowed(player.Handle())
	return res, nil
}

// AttachCameraToObject wraps CAPI Player_AttachCameraToObject
func (a *API) AttachCameraToObject(player *omp.Player, object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Player_AttachCameraToObject(player.Handle(), object.Handle())
	return res, nil
}

// AttachCameraToPlayerObject wraps CAPI Player_AttachCameraToPlayerObject
func (a *API) AttachCameraToPlayerObject(player *omp.Player, object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Player_AttachCameraToPlayerObject(player.Handle(), object.Handle())
	return res, nil
}

// AttachObjectToPlayer wraps CAPI Object_AttachToPlayer
func (a *API) AttachObjectToPlayer(object *omp.Object, player *omp.Player, offsetX float32, offsetY float32, offsetZ float32, rotationX float32, rotationY float32, rotationZ float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Object_AttachToPlayer(object.Handle(), player.Handle(), offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ)
	return res, nil
}

// AttachObjectToVehicle wraps CAPI Object_AttachToVehicle
func (a *API) AttachObjectToVehicle(object *omp.Object, vehicle *omp.Vehicle, offsetX float32, offsetY float32, offsetZ float32, rotationX float32, rotationY float32, rotationZ float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Object_AttachToVehicle(object.Handle(), vehicle.Handle(), offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ)
	return res, nil
}

// AttachPlayerObjectToObject wraps CAPI PlayerObject_AttachToObject
func (a *API) AttachPlayerObjectToObject(player *omp.Player, object *omp.PlayerObject, attachedTo *omp.PlayerObject, offsetX float32, offsetY float32, offsetZ float32, rotationX float32, rotationY float32, rotationZ float32, syncRotation bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	if attachedTo == nil || !attachedTo.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_AttachToObject(player.Handle(), object.Handle(), attachedTo.Handle(), offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ, syncRotation)
	return res, nil
}

// AttachPlayerObjectToPlayer wraps CAPI PlayerObject_AttachToPlayer
func (a *API) AttachPlayerObjectToPlayer(player *omp.Player, object *omp.PlayerObject, playerAttachedTo *omp.Player, offsetX float32, offsetY float32, offsetZ float32, rotationX float32, rotationY float32, rotationZ float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	if playerAttachedTo == nil || !playerAttachedTo.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.PlayerObject_AttachToPlayer(player.Handle(), object.Handle(), playerAttachedTo.Handle(), offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ)
	return res, nil
}

// AttachPlayerObjectToVehicle wraps CAPI PlayerObject_AttachToVehicle
func (a *API) AttachPlayerObjectToVehicle(player *omp.Player, object *omp.PlayerObject, vehicle *omp.Vehicle, offsetX float32, offsetY float32, offsetZ float32, rotationX float32, rotationY float32, rotationZ float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.PlayerObject_AttachToVehicle(player.Handle(), object.Handle(), vehicle.Handle(), offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ)
	return res, nil
}

// AttachTextLabelToPlayer wraps CAPI TextLabel_AttachToPlayer
func (a *API) AttachTextLabelToPlayer(textlabel *omp.TextLabel, player *omp.Player, offsetX float32, offsetY float32, offsetZ float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.TextLabel_AttachToPlayer(textlabel.Handle(), player.Handle(), offsetX, offsetY, offsetZ)
	return res, nil
}

// AttachTextLabelToVehicle wraps CAPI TextLabel_AttachToVehicle
func (a *API) AttachTextLabelToVehicle(textlabel *omp.TextLabel, vehicle *omp.Vehicle, offsetX float32, offsetY float32, offsetZ float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.TextLabel_AttachToVehicle(textlabel.Handle(), vehicle.Handle(), offsetX, offsetY, offsetZ)
	return res, nil
}

// AttachToObject wraps CAPI Object_AttachToObject
func (a *API) AttachToObject(object *omp.Object, objAttachedTo *omp.Object, offsetX float32, offsetY float32, offsetZ float32, rotationX float32, rotationY float32, rotationZ float32, syncRotation bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	if objAttachedTo == nil || !objAttachedTo.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_AttachToObject(object.Handle(), objAttachedTo.Handle(), offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ, syncRotation)
	return res, nil
}

// AttachTrailer wraps CAPI Vehicle_AttachTrailer
func (a *API) AttachTrailer(trailer *omp.Vehicle, vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if trailer == nil || !trailer.Valid() {
		return false, ErrInvalidVehicle
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_AttachTrailer(trailer.Handle(), vehicle.Handle())
	return res, nil
}

// Ban wraps CAPI Player_Ban
func (a *API) Ban(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_Ban(player.Handle())
	return res, nil
}

// BanEx wraps CAPI Player_BanEx
func (a *API) BanEx(player *omp.Player, reason string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_BanEx(player.Handle(), reason)
	return res, nil
}

// BeginSelecting wraps CAPI Object_BeginSelecting
func (a *API) BeginSelecting(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Object_BeginSelecting(player.Handle())
	return res, nil
}

// BlockIPAddress wraps CAPI Core_BlockIpAddress
func (a *API) BlockIPAddress(ipAddress string, timeMS int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_BlockIpAddress(ipAddress, timeMS)
	return res, nil
}

// CanHaveComponent wraps CAPI Vehicle_CanHaveComponent
func (a *API) CanHaveComponent(modelid int32, componentid int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Vehicle_CanHaveComponent(modelid, componentid)
	return res, nil
}

// CancelSelectTextDraw wraps CAPI Player_CancelSelectTextDraw
func (a *API) CancelSelectTextDraw(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_CancelSelectTextDraw(player.Handle())
	return res, nil
}

// ChangeColor wraps CAPI Vehicle_ChangeColor
func (a *API) ChangeColor(vehicle *omp.Vehicle, color1 int32, color2 int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_ChangeColor(vehicle.Handle(), color1, color2)
	return res, nil
}

// ChangePaintjob wraps CAPI Vehicle_ChangePaintjob
func (a *API) ChangePaintjob(vehicle *omp.Vehicle, paintjobid int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_ChangePaintjob(vehicle.Handle(), paintjobid)
	return res, nil
}

// ClassFromID wraps CAPI Class_FromID
func (a *API) ClassFromID(classid int32) (*omp.Class, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Class_FromID(classid)
	return omp.NewClass(res), nil
}

// ClearActorAnimations wraps CAPI Actor_ClearAnimations
func (a *API) ClearActorAnimations(actor *omp.Actor) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_ClearAnimations(actor.Handle())
	return res, nil
}

// ClearBanList wraps CAPI Core_ClearBanList
func (a *API) ClearBanList() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_ClearBanList()
	return res, nil
}

// ClearPlayerAnimations wraps CAPI Player_ClearAnimations
func (a *API) ClearPlayerAnimations(player *omp.Player, syncType int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ClearAnimations(player.Handle(), syncType)
	return res, nil
}

// ClearWorldBounds wraps CAPI Player_ClearWorldBounds
func (a *API) ClearWorldBounds(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ClearWorldBounds(player.Handle())
	return res, nil
}

// ColorIndexToColor wraps CAPI Vehicle_ColorIndexToColor
func (a *API) ColorIndexToColor(colorIndex int32, alpha int32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Vehicle_ColorIndexToColor(colorIndex, alpha)
	return res, nil
}

// Count wraps CAPI Class_Count
func (a *API) Count() (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Class_Count()
	return res, nil
}

// CountOccupants wraps CAPI Vehicle_CountOccupants
func (a *API) CountOccupants(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_CountOccupants(vehicle.Handle())
	return res, nil
}

// CreateActor wraps CAPI Actor_Create
func (a *API) CreateActor(model int32, x float32, y float32, z float32, rot float32, id *int32) (*omp.Actor, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Actor_Create(model, x, y, z, rot, id)
	return omp.NewActor(res), nil
}

// CreateAllExplosion wraps CAPI All_CreateExplosion
func (a *API) CreateAllExplosion(x float32, y float32, z float32, typ int32, radius float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.All_CreateExplosion(x, y, z, typ, radius)
	return res, nil
}

// CreateGangZone wraps CAPI GangZone_Create
func (a *API) CreateGangZone(minx float32, miny float32, maxx float32, maxy float32, id *int32) (*omp.GangZone, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.GangZone_Create(minx, miny, maxx, maxy, id)
	return omp.NewGangZone(res), nil
}

// CreateMenu wraps CAPI Menu_Create
func (a *API) CreateMenu(title string, columns uint32, x float32, y float32, column1Width float32, column2Width float32, id *int32) (*omp.Menu, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Menu_Create(title, columns, x, y, column1Width, column2Width, id)
	return omp.NewMenu(res), nil
}

// CreateObject wraps CAPI Object_Create
func (a *API) CreateObject(modelid int32, x float32, y float32, z float32, rotationX float32, rotationY float32, rotationZ float32, drawDistance float32, id *int32) (*omp.Object, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Object_Create(modelid, x, y, z, rotationX, rotationY, rotationZ, drawDistance, id)
	return omp.NewObject(res), nil
}

// CreatePickup wraps CAPI Pickup_Create
func (a *API) CreatePickup(model int32, typ int32, x float32, y float32, z float32, virtualWorld int32, id *int32) (*omp.Pickup, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Pickup_Create(model, typ, x, y, z, virtualWorld, id)
	return omp.NewPickup(res), nil
}

// CreatePlayerExplosion wraps CAPI Player_CreateExplosion
func (a *API) CreatePlayerExplosion(player *omp.Player, x float32, y float32, z float32, typ int32, radius float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_CreateExplosion(player.Handle(), x, y, z, typ, radius)
	return res, nil
}

// CreatePlayerObject wraps CAPI PlayerObject_Create
func (a *API) CreatePlayerObject(player *omp.Player, modelid int32, x float32, y float32, z float32, rotationX float32, rotationY float32, rotationZ float32, drawDistance float32, id *int32) (*omp.PlayerObject, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.PlayerObject_Create(player.Handle(), modelid, x, y, z, rotationX, rotationY, rotationZ, drawDistance, id)
	return omp.NewPlayerObject(res), nil
}

// CreatePlayerTextDraw wraps CAPI PlayerTextDraw_Create
func (a *API) CreatePlayerTextDraw(player *omp.Player, x float32, y float32, text string, id *int32) (*omp.PlayerTextDraw, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.PlayerTextDraw_Create(player.Handle(), x, y, text, id)
	return omp.NewPlayerTextDraw(res), nil
}

// CreatePlayerTextLabel wraps CAPI PlayerTextLabel_Create
func (a *API) CreatePlayerTextLabel(player *omp.Player, text string, color uint32, x float32, y float32, z float32, drawDistance float32, attachedPlayer *omp.Player, attachedVehicle *omp.Vehicle, los bool, id *int32) (*omp.PlayerTextLabel, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	if attachedPlayer == nil || !attachedPlayer.Valid() {
		return nil, ErrInvalidPlayer
	}
	if attachedVehicle == nil || !attachedVehicle.Valid() {
		return nil, ErrInvalidVehicle
	}
	res := a.capi.PlayerTextLabel_Create(player.Handle(), text, color, x, y, z, drawDistance, attachedPlayer.Handle(), attachedVehicle.Handle(), los, id)
	return omp.NewPlayerTextLabel(res), nil
}

// CreateTextDraw wraps CAPI TextDraw_Create
func (a *API) CreateTextDraw(x float32, y float32, text string, id *int32) (*omp.TextDraw, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.TextDraw_Create(x, y, text, id)
	return omp.NewTextDraw(res), nil
}

// CreateTextLabel wraps CAPI TextLabel_Create
func (a *API) CreateTextLabel(text string, color uint32, x float32, y float32, z float32, drawDistance float32, virtualWorld int32, los bool, id *int32) (*omp.TextLabel, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.TextLabel_Create(text, color, x, y, z, drawDistance, virtualWorld, los, id)
	return omp.NewTextLabel(res), nil
}

// CreateVehicle wraps CAPI Vehicle_Create
func (a *API) CreateVehicle(modelid int32, x float32, y float32, z float32, rotation float32, color1 int32, color2 int32, respawnDelay int32, addSiren bool, id *int32) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Vehicle_Create(modelid, x, y, z, rotation, color1, color2, respawnDelay, addSiren, id)
	return omp.NewVehicle(res), nil
}

// DestroyActor wraps CAPI Actor_Destroy
func (a *API) DestroyActor(actor *omp.Actor) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_Destroy(actor.Handle())
	return res, nil
}

// DestroyGangZone wraps CAPI GangZone_Destroy
func (a *API) DestroyGangZone(gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_Destroy(gangzone.Handle())
	return res, nil
}

// DestroyMenu wraps CAPI Menu_Destroy
func (a *API) DestroyMenu(menu *omp.Menu) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_Destroy(menu.Handle())
	return res, nil
}

// DestroyObject wraps CAPI Object_Destroy
func (a *API) DestroyObject(object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_Destroy(object.Handle())
	return res, nil
}

// DestroyPickup wraps CAPI Pickup_Destroy
func (a *API) DestroyPickup(pickup *omp.Pickup) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_Destroy(pickup.Handle())
	return res, nil
}

// DestroyPlayerObject wraps CAPI PlayerObject_Destroy
func (a *API) DestroyPlayerObject(player *omp.Player, object *omp.PlayerObject) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_Destroy(player.Handle(), object.Handle())
	return res, nil
}

// DestroyPlayerTextDraw wraps CAPI PlayerTextDraw_Destroy
func (a *API) DestroyPlayerTextDraw(player *omp.Player, textdraw *omp.PlayerTextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_Destroy(player.Handle(), textdraw.Handle())
	return res, nil
}

// DestroyPlayerTextLabel wraps CAPI PlayerTextLabel_Destroy
func (a *API) DestroyPlayerTextLabel(player *omp.Player, textlabel *omp.PlayerTextLabel) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_Destroy(player.Handle(), textlabel.Handle())
	return res, nil
}

// DestroyTextDraw wraps CAPI TextDraw_Destroy
func (a *API) DestroyTextDraw(textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_Destroy(textdraw.Handle())
	return res, nil
}

// DestroyTextLabel wraps CAPI TextLabel_Destroy
func (a *API) DestroyTextLabel(textlabel *omp.TextLabel) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_Destroy(textlabel.Handle())
	return res, nil
}

// DestroyVehicle wraps CAPI Vehicle_Destroy
func (a *API) DestroyVehicle(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_Destroy(vehicle.Handle())
	return res, nil
}

// DetachTrailer wraps CAPI Vehicle_DetachTrailer
func (a *API) DetachTrailer(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_DetachTrailer(vehicle.Handle())
	return res, nil
}

// DisableCheckpoint wraps CAPI Checkpoint_Disable
func (a *API) DisableCheckpoint(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Checkpoint_Disable(player.Handle())
	return res, nil
}

// DisableEntryExitMarkers wraps CAPI Core_DisableEntryExitMarkers
func (a *API) DisableEntryExitMarkers() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_DisableEntryExitMarkers()
	return res, nil
}

// DisableMenu wraps CAPI Menu_Disable
func (a *API) DisableMenu(menu *omp.Menu) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_Disable(menu.Handle())
	return res, nil
}

// DisableNameTagsLOS wraps CAPI Core_DisableNameTagsLOS
func (a *API) DisableNameTagsLOS() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_DisableNameTagsLOS()
	return res, nil
}

// DisableRaceCheckpoint wraps CAPI RaceCheckpoint_Disable
func (a *API) DisableRaceCheckpoint(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.RaceCheckpoint_Disable(player.Handle())
	return res, nil
}

// DisableRemoteVehicleCollisions wraps CAPI Player_DisableRemoteVehicleCollisions
func (a *API) DisableRemoteVehicleCollisions(player *omp.Player, disable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_DisableRemoteVehicleCollisions(player.Handle(), disable)
	return res, nil
}

// DisableRow wraps CAPI Menu_DisableRow
func (a *API) DisableRow(menu *omp.Menu, row uint8) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_DisableRow(menu.Handle(), row)
	return res, nil
}

// Edit wraps CAPI Class_Edit
func (a *API) Edit(classptr *omp.Class, teamid uint8, skin int32, x float32, y float32, z float32, angle float32, weapon1 uint8, ammo1 uint32, weapon2 uint8, ammo2 uint32, weapon3 uint8, ammo3 uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if classptr == nil || !classptr.Valid() {
		return false, ErrInvalidClass
	}
	res := a.capi.Class_Edit(classptr.Handle(), teamid, skin, x, y, z, angle, weapon1, ammo1, weapon2, ammo2, weapon3, ammo3)
	return res, nil
}

// EditAttachedObject wraps CAPI Player_EditAttachedObject
func (a *API) EditAttachedObject(player *omp.Player, index int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_EditAttachedObject(player.Handle(), index)
	return res, nil
}

// EnableAllAnimations wraps CAPI Core_EnableAllAnimations
func (a *API) EnableAllAnimations(allow bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_EnableAllAnimations(allow)
	return res, nil
}

// EnableAllStuntBonus wraps CAPI All_EnableStuntBonus
func (a *API) EnableAllStuntBonus(enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.All_EnableStuntBonus(enable)
	return res, nil
}

// EnableCameraTarget wraps CAPI Player_EnableCameraTarget
func (a *API) EnableCameraTarget(player *omp.Player, enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_EnableCameraTarget(player.Handle(), enable)
	return res, nil
}

// EnableFriendlyFire wraps CAPI Vehicle_EnableFriendlyFire
func (a *API) EnableFriendlyFire() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Vehicle_EnableFriendlyFire()
	return res, nil
}

// EnablePlayerStuntBonus wraps CAPI Player_EnableStuntBonus
func (a *API) EnablePlayerStuntBonus(player *omp.Player, enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_EnableStuntBonus(player.Handle(), enable)
	return res, nil
}

// EnableZoneNames wraps CAPI Core_EnableZoneNames
func (a *API) EnableZoneNames(enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_EnableZoneNames(enable)
	return res, nil
}

// EndEditing wraps CAPI Object_EndEditing
func (a *API) EndEditing(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Object_EndEditing(player.Handle())
	return res, nil
}

// FlashForAll wraps CAPI GangZone_FlashForAll
func (a *API) FlashForAll(gangzone *omp.GangZone, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_FlashForAll(gangzone.Handle(), color)
	return res, nil
}

// FlashForPlayer wraps CAPI GangZone_FlashForPlayer
func (a *API) FlashForPlayer(player *omp.Player, gangzone *omp.GangZone, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_FlashForPlayer(player.Handle(), gangzone.Handle(), color)
	return res, nil
}

// ForceClassSelection wraps CAPI Player_ForceClassSelection
func (a *API) ForceClassSelection(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ForceClassSelection(player.Handle())
	return res, nil
}

// GPCI wraps CAPI Player_GPCI
func (a *API) GPCI(player *omp.Player) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return "", ErrInvalidPlayer
	}
	res, ok := a.capi.Player_GPCIString(player.Handle())
	if !ok {
		return "", errors.New("gamemode: GPCI failed")
	}
	return res, nil
}

// GameModeSetText wraps CAPI Core_GameMode_SetText
func (a *API) GameModeSetText(string string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_GameMode_SetText(string)
	return res, nil
}

// GangZoneFromID wraps CAPI GangZone_FromID
func (a *API) GangZoneFromID(gangzoneid int32) (*omp.GangZone, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.GangZone_FromID(gangzoneid)
	return omp.NewGangZone(res), nil
}

// GetActorFacingAngle wraps CAPI Actor_GetFacingAngle
func (a *API) GetActorFacingAngle(actor *omp.Actor) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return 0, ErrInvalidActor
	}
	res := a.capi.Actor_GetFacingAngle(actor.Handle())
	return res, nil
}

// GetActorHealth wraps CAPI Actor_GetHealth
func (a *API) GetActorHealth(actor *omp.Actor) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return 0, ErrInvalidActor
	}
	res := a.capi.Actor_GetHealth(actor.Handle())
	return res, nil
}

// GetActorID wraps CAPI Actor_GetID
func (a *API) GetActorID(actor *omp.Actor) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return 0, ErrInvalidActor
	}
	res := a.capi.Actor_GetID(actor.Handle())
	return res, nil
}

// GetActorPos wraps CAPI Actor_GetPos
func (a *API) GetActorPos(actor *omp.Actor, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_GetPos(actor.Handle(), x, y, z)
	return res, nil
}

// GetActorSkin wraps CAPI Actor_GetSkin
func (a *API) GetActorSkin(actor *omp.Actor) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return 0, ErrInvalidActor
	}
	res := a.capi.Actor_GetSkin(actor.Handle())
	return res, nil
}

// GetActorSpawnInfo wraps CAPI Actor_GetSpawnInfo
func (a *API) GetActorSpawnInfo(actor *omp.Actor, x *float32, y *float32, z *float32, angle *float32, skin *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_GetSpawnInfo(actor.Handle(), x, y, z, angle, skin)
	return res, nil
}

// GetActorVirtualWorld wraps CAPI Actor_GetVirtualWorld
func (a *API) GetActorVirtualWorld(actor *omp.Actor) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return 0, ErrInvalidActor
	}
	res := a.capi.Actor_GetVirtualWorld(actor.Handle())
	return res, nil
}

// GetAnimation wraps CAPI Actor_GetAnimation
func (a *API) GetAnimation(actor *omp.Actor, library *string, name *string, delta *float32, loop *bool, lockX *bool, lockY *bool, freeze *bool, time *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	var libraryView runtime.CAPIStringView
	var nameView runtime.CAPIStringView
	res := a.capi.Actor_GetAnimation(actor.Handle(), &libraryView, &nameView, delta, loop, lockX, lockY, freeze, time)
	if library != nil {
		*library = runtime.CAPIStringViewToString(&libraryView)
	}
	if name != nil {
		*name = runtime.CAPIStringViewToString(&nameView)
	}
	return res, nil
}

// GetAnimationFlags wraps CAPI Player_GetAnimationFlags
func (a *API) GetAnimationFlags(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetAnimationFlags(player.Handle())
	return res, nil
}

// GetAnimationIndex wraps CAPI Player_GetAnimationIndex
func (a *API) GetAnimationIndex(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetAnimationIndex(player.Handle())
	return res, nil
}

// GetAnimationName wraps CAPI Player_GetAnimationName
func (a *API) GetAnimationName(index int32, lib *string, name *string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	var libView runtime.CAPIStringView
	var nameView runtime.CAPIStringView
	res := a.capi.Player_GetAnimationName(index, &libView, &nameView)
	if lib != nil {
		*lib = runtime.CAPIStringViewToString(&libView)
	}
	if name != nil {
		*name = runtime.CAPIStringViewToString(&nameView)
	}
	return res, nil
}

// GetArmor wraps CAPI Player_GetArmor
func (a *API) GetArmor(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetArmor(player.Handle())
	return res, nil
}

// GetAsBool wraps CAPI Config_GetAsBool
func (a *API) GetAsBool(cvar string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Config_GetAsBool(cvar)
	return res, nil
}

// GetAsFloat wraps CAPI Config_GetAsFloat
func (a *API) GetAsFloat(cvar string) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Config_GetAsFloat(cvar)
	return res, nil
}

// GetAsInt wraps CAPI Config_GetAsInt
func (a *API) GetAsInt(cvar string) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Config_GetAsInt(cvar)
	return res, nil
}

// GetAsString wraps CAPI Config_GetAsString
func (a *API) GetAsString(cvar string) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	res, ok := a.capi.Config_GetAsStringString(cvar)
	if !ok {
		return "", errors.New("gamemode: GetAsString failed")
	}
	return res, nil
}

// GetAttachedObject wraps CAPI Player_GetAttachedObject
func (a *API) GetAttachedObject(player *omp.Player, index int32, modelid *int32, bone *int32, offsetX *float32, offsetY *float32, offsetZ *float32, rotationX *float32, rotationY *float32, rotationZ *float32, scaleX *float32, scaleY *float32, scaleZ *float32, materialcolor1 *int32, materialcolor2 *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetAttachedObject(player.Handle(), index, modelid, bone, offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ, scaleX, scaleY, scaleZ, materialcolor1, materialcolor2)
	return res, nil
}

// GetBuildingsRemoved wraps CAPI Player_GetBuildingsRemoved
func (a *API) GetBuildingsRemoved(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetBuildingsRemoved(player.Handle())
	return res, nil
}

// GetCab wraps CAPI Vehicle_GetCab
func (a *API) GetCab(vehicle *omp.Vehicle) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return nil, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetCab(vehicle.Handle())
	return omp.NewVehicle(res), nil
}

// GetCameraAspectRatio wraps CAPI Player_GetCameraAspectRatio
func (a *API) GetCameraAspectRatio(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraAspectRatio(player.Handle())
	return res, nil
}

// GetCameraFrontVector wraps CAPI Player_GetCameraFrontVector
func (a *API) GetCameraFrontVector(player *omp.Player, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraFrontVector(player.Handle(), x, y, z)
	return res, nil
}

// GetCameraMode wraps CAPI Player_GetCameraMode
func (a *API) GetCameraMode(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraMode(player.Handle())
	return res, nil
}

// GetCameraPos wraps CAPI Player_GetCameraPos
func (a *API) GetCameraPos(player *omp.Player, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraPos(player.Handle(), x, y, z)
	return res, nil
}

// GetCameraTargetActor wraps CAPI Player_GetCameraTargetActor
func (a *API) GetCameraTargetActor(player *omp.Player) (*omp.Actor, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraTargetActor(player.Handle())
	return omp.NewActor(res), nil
}

// GetCameraTargetObject wraps CAPI Player_GetCameraTargetObject
func (a *API) GetCameraTargetObject(player *omp.Player) (*omp.Object, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraTargetObject(player.Handle())
	return omp.NewObject(res), nil
}

// GetCameraTargetPlayer wraps CAPI Player_GetCameraTargetPlayer
func (a *API) GetCameraTargetPlayer(player *omp.Player) (*omp.Player, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraTargetPlayer(player.Handle())
	return omp.NewPlayer(res), nil
}

// GetCameraTargetPlayerObject wraps CAPI Player_GetCameraTargetPlayerObject
func (a *API) GetCameraTargetPlayerObject(player *omp.Player) (*omp.PlayerObject, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraTargetPlayerObject(player.Handle())
	return omp.NewPlayerObject(res), nil
}

// GetCameraTargetVehicle wraps CAPI Player_GetCameraTargetVehicle
func (a *API) GetCameraTargetVehicle(player *omp.Player) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraTargetVehicle(player.Handle())
	return omp.NewVehicle(res), nil
}

// GetCameraZoom wraps CAPI Player_GetCameraZoom
func (a *API) GetCameraZoom(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCameraZoom(player.Handle())
	return res, nil
}

// GetCheckpoint wraps CAPI Checkpoint_Get
func (a *API) GetCheckpoint(player *omp.Player, x *float32, y *float32, z *float32, radius *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Checkpoint_Get(player.Handle(), x, y, z, radius)
	return res, nil
}

// GetClassID wraps CAPI Class_GetID
func (a *API) GetClassID(cls *omp.Class) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if cls == nil || !cls.Valid() {
		return 0, ErrInvalidClass
	}
	res := a.capi.Class_GetID(cls.Handle())
	return res, nil
}

// GetColorForPlayer wraps CAPI GangZone_GetColorForPlayer
func (a *API) GetColorForPlayer(player *omp.Player, gangzone *omp.GangZone) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return 0, ErrInvalidGangZone
	}
	res := a.capi.GangZone_GetColorForPlayer(player.Handle(), gangzone.Handle())
	return res, nil
}

// GetColumnHeader wraps CAPI Menu_GetColumnHeader
func (a *API) GetColumnHeader(menu *omp.Menu, column int32) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return "", ErrInvalidMenu
	}
	res, ok := a.capi.Menu_GetColumnHeaderString(menu.Handle(), column)
	if !ok {
		return "", errors.New("gamemode: GetColumnHeader failed")
	}
	return res, nil
}

// GetColumnWidth wraps CAPI Menu_GetColumnWidth
func (a *API) GetColumnWidth(menu *omp.Menu, column1Width *float32, column2Width *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_GetColumnWidth(menu.Handle(), column1Width, column2Width)
	return res, nil
}

// GetColumns wraps CAPI Menu_GetColumns
func (a *API) GetColumns(menu *omp.Menu) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return 0, ErrInvalidMenu
	}
	res := a.capi.Menu_GetColumns(menu.Handle())
	return res, nil
}

// GetComponentInSlot wraps CAPI Vehicle_GetComponentInSlot
func (a *API) GetComponentInSlot(vehicle *omp.Vehicle, slot int32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetComponentInSlot(vehicle.Handle(), slot)
	return res, nil
}

// GetComponentType wraps CAPI Vehicle_GetComponentType
func (a *API) GetComponentType(componentid int32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Vehicle_GetComponentType(componentid)
	return res, nil
}

// GetCoreGravity wraps CAPI Core_GetGravity
func (a *API) GetCoreGravity() (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Core_GetGravity()
	return res, nil
}

// GetCoreWeather wraps CAPI Core_GetWeather
func (a *API) GetCoreWeather() (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Core_GetWeather()
	return res, nil
}

// GetCustomSkin wraps CAPI Player_GetCustomSkin
func (a *API) GetCustomSkin(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetCustomSkin(player.Handle())
	return res, nil
}

// GetDamageStatus wraps CAPI Vehicle_GetDamageStatus
func (a *API) GetDamageStatus(vehicle *omp.Vehicle, panels *int32, doors *int32, lights *int32, tires *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetDamageStatus(vehicle.Handle(), panels, doors, lights, tires)
	return res, nil
}

// GetData wraps CAPI Class_GetData
func (a *API) GetData(classptr *omp.Class, teamid *uint8, skin *int32, x *float32, y *float32, z *float32, angle *float32, weapon1 *uint8, weapon1Ammo *uint32, weapon2 *uint8, weapon2Ammo *uint32, weapon3 *uint8, weapon3Ammo *uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if classptr == nil || !classptr.Valid() {
		return false, ErrInvalidClass
	}
	res := a.capi.Class_GetData(classptr.Handle(), teamid, skin, x, y, z, angle, weapon1, weapon1Ammo, weapon2, weapon2Ammo, weapon3, weapon3Ammo)
	return res, nil
}

// GetDefaultColor wraps CAPI Player_GetDefaultColor
func (a *API) GetDefaultColor(player *omp.Player) (uint32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetDefaultColor(player.Handle())
	return res, nil
}

// GetDialog wraps CAPI Player_GetDialog
func (a *API) GetDialog(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetDialog(player.Handle())
	return res, nil
}

// GetDialogData wraps CAPI Player_GetDialogData
func (a *API) GetDialogData(player *omp.Player, dialogid *int32, style *int32, title *string, body *string, button1 *string, button2 *string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	var titleView runtime.CAPIStringView
	var bodyView runtime.CAPIStringView
	var button1View runtime.CAPIStringView
	var button2View runtime.CAPIStringView
	res := a.capi.Player_GetDialogData(player.Handle(), dialogid, style, &titleView, &bodyView, &button1View, &button2View)
	if title != nil {
		*title = runtime.CAPIStringViewToString(&titleView)
	}
	if body != nil {
		*body = runtime.CAPIStringViewToString(&bodyView)
	}
	if button1 != nil {
		*button1 = runtime.CAPIStringViewToString(&button1View)
	}
	if button2 != nil {
		*button2 = runtime.CAPIStringViewToString(&button2View)
	}
	return res, nil
}

// GetDriver wraps CAPI Vehicle_GetDriver
func (a *API) GetDriver(vehicle *omp.Vehicle) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return nil, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetDriver(vehicle.Handle())
	return omp.NewVehicle(res), nil
}

// GetDrunkLevel wraps CAPI Player_GetDrunkLevel
func (a *API) GetDrunkLevel(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetDrunkLevel(player.Handle())
	return res, nil
}

// GetFightingStyle wraps CAPI Player_GetFightingStyle
func (a *API) GetFightingStyle(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetFightingStyle(player.Handle())
	return res, nil
}

// GetFlashColorForPlayer wraps CAPI GangZone_GetFlashColorForPlayer
func (a *API) GetFlashColorForPlayer(player *omp.Player, gangzone *omp.GangZone) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return 0, ErrInvalidGangZone
	}
	res := a.capi.GangZone_GetFlashColorForPlayer(player.Handle(), gangzone.Handle())
	return res, nil
}

// GetGameText wraps CAPI Player_GetGameText
func (a *API) GetGameText(player *omp.Player, style int32, message *string, time *int32, remaining *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	var messageView runtime.CAPIStringView
	res := a.capi.Player_GetGameText(player.Handle(), style, &messageView, time, remaining)
	if message != nil {
		*message = runtime.CAPIStringViewToString(&messageView)
	}
	return res, nil
}

// GetGangZoneID wraps CAPI GangZone_GetID
func (a *API) GetGangZoneID(gangzone *omp.GangZone) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return 0, ErrInvalidGangZone
	}
	res := a.capi.GangZone_GetID(gangzone.Handle())
	return res, nil
}

// GetGangZonePos wraps CAPI GangZone_GetPos
func (a *API) GetGangZonePos(gangzone *omp.GangZone, minx *float32, miny *float32, maxx *float32, maxy *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_GetPos(gangzone.Handle(), minx, miny, maxx, maxy)
	return res, nil
}

// GetGhostMode wraps CAPI Player_GetGhostMode
func (a *API) GetGhostMode(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetGhostMode(player.Handle())
	return res, nil
}

// GetIP wraps CAPI Player_GetIp
func (a *API) GetIP(player *omp.Player) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return "", ErrInvalidPlayer
	}
	res, ok := a.capi.Player_GetIpString(player.Handle())
	if !ok {
		return "", errors.New("gamemode: GetIP failed")
	}
	return res, nil
}

// GetItem wraps CAPI Menu_GetItem
func (a *API) GetItem(menu *omp.Menu, column int32, row int32) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return "", ErrInvalidMenu
	}
	res, ok := a.capi.Menu_GetItemString(menu.Handle(), column, row)
	if !ok {
		return "", errors.New("gamemode: GetItem failed")
	}
	return res, nil
}

// GetItems wraps CAPI Menu_GetItems
func (a *API) GetItems(menu *omp.Menu, column int32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return 0, ErrInvalidMenu
	}
	res := a.capi.Menu_GetItems(menu.Handle(), column)
	return res, nil
}

// GetKeys wraps CAPI Player_GetKeys
func (a *API) GetKeys(player *omp.Player, keys *int32, updown *int32, leftright *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetKeys(player.Handle(), keys, updown, leftright)
	return res, nil
}

// GetLastDriver wraps CAPI Vehicle_GetLastDriver
func (a *API) GetLastDriver(vehicle *omp.Vehicle) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return nil, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetLastDriver(vehicle.Handle())
	return omp.NewVehicle(res), nil
}

// GetLastShotVectors wraps CAPI Player_GetLastShotVectors
func (a *API) GetLastShotVectors(player *omp.Player, originX *float32, originY *float32, originZ *float32, hitX *float32, hitY *float32, hitZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetLastShotVectors(player.Handle(), originX, originY, originZ, hitX, hitY, hitZ)
	return res, nil
}

// GetMarkerForPlayer wraps CAPI Player_GetMarkerForPlayer
func (a *API) GetMarkerForPlayer(player *omp.Player, other *omp.Player) (uint32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if other == nil || !other.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetMarkerForPlayer(player.Handle(), other.Handle())
	return res, nil
}

// GetMatrix wraps CAPI Vehicle_GetMatrix
func (a *API) GetMatrix(vehicle *omp.Vehicle, rightX *float32, rightY *float32, rightZ *float32, upX *float32, upY *float32, upZ *float32, atX *float32, atY *float32, atZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetMatrix(vehicle.Handle(), rightX, rightY, rightZ, upX, upY, upZ, atX, atY, atZ)
	return res, nil
}

// GetMaxPassengerSeats wraps CAPI Vehicle_GetMaxPassengerSeats
func (a *API) GetMaxPassengerSeats(modelid int32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Vehicle_GetMaxPassengerSeats(modelid)
	return res, nil
}

// GetMenu wraps CAPI Player_GetMenu
func (a *API) GetMenu(player *omp.Player) (*omp.Menu, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetMenu(player.Handle())
	return omp.NewMenu(res), nil
}

// GetMenuID wraps CAPI Menu_GetID
func (a *API) GetMenuID(menu *omp.Menu) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return 0, ErrInvalidMenu
	}
	res := a.capi.Menu_GetID(menu.Handle())
	return res, nil
}

// GetMenuPos wraps CAPI Menu_GetPos
func (a *API) GetMenuPos(menu *omp.Menu, x *float32, y *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_GetPos(menu.Handle(), x, y)
	return res, nil
}

// GetModelCount wraps CAPI Vehicle_GetModelCount
func (a *API) GetModelCount(modelid int32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Vehicle_GetModelCount(modelid)
	return res, nil
}

// GetModelInfo wraps CAPI Vehicle_GetModelInfo
func (a *API) GetModelInfo(vehiclemodel int32, infotype int32, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Vehicle_GetModelInfo(vehiclemodel, infotype, x, y, z)
	return res, nil
}

// GetModelsUsed wraps CAPI Vehicle_GetModelsUsed
func (a *API) GetModelsUsed() (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Vehicle_GetModelsUsed()
	return res, nil
}

// GetMoney wraps CAPI Player_GetMoney
func (a *API) GetMoney(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetMoney(player.Handle())
	return res, nil
}

// GetName wraps CAPI Player_GetName
func (a *API) GetName(player *omp.Player) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return "", ErrInvalidPlayer
	}
	res, ok := a.capi.Player_GetNameString(player.Handle())
	if !ok {
		return "", errors.New("gamemode: GetName failed")
	}
	return res, nil
}

// GetNetworkStats wraps CAPI Player_GetNetworkStats
func (a *API) GetNetworkStats(player *omp.Player) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return "", ErrInvalidPlayer
	}
	res, ok := a.capi.Player_GetNetworkStatsString(player.Handle())
	if !ok {
		return "", errors.New("gamemode: GetNetworkStats failed")
	}
	return res, nil
}

// GetNumberPlate wraps CAPI Vehicle_GetNumberPlate
func (a *API) GetNumberPlate(vehicle *omp.Vehicle) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return "", ErrInvalidVehicle
	}
	res, ok := a.capi.Vehicle_GetNumberPlateString(vehicle.Handle())
	if !ok {
		return "", errors.New("gamemode: GetNumberPlate failed")
	}
	return res, nil
}

// GetObjectAttachedData wraps CAPI Object_GetAttachedData
func (a *API) GetObjectAttachedData(object *omp.Object, parentVehicle *int32, parentObject *int32, parentPlayer *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_GetAttachedData(object.Handle(), parentVehicle, parentObject, parentPlayer)
	return res, nil
}

// GetObjectAttachedOffset wraps CAPI Object_GetAttachedOffset
func (a *API) GetObjectAttachedOffset(object *omp.Object, offsetX *float32, offsetY *float32, offsetZ *float32, rotationX *float32, rotationY *float32, rotationZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_GetAttachedOffset(object.Handle(), offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ)
	return res, nil
}

// GetObjectDrawDistance wraps CAPI Object_GetDrawDistance
func (a *API) GetObjectDrawDistance(object *omp.Object) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidObject
	}
	res := a.capi.Object_GetDrawDistance(object.Handle())
	return res, nil
}

// GetObjectID wraps CAPI Object_GetID
func (a *API) GetObjectID(object *omp.Object) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidObject
	}
	res := a.capi.Object_GetID(object.Handle())
	return res, nil
}

// GetObjectMaterial wraps CAPI Object_GetMaterial
func (a *API) GetObjectMaterial(object *omp.Object, materialIndex int32, modelid *int32, textureLibrary *string, textureName *string, materialColor *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	var textureLibraryView runtime.CAPIStringView
	var textureNameView runtime.CAPIStringView
	res := a.capi.Object_GetMaterial(object.Handle(), materialIndex, modelid, &textureLibraryView, &textureNameView, materialColor)
	if textureLibrary != nil {
		*textureLibrary = runtime.CAPIStringViewToString(&textureLibraryView)
	}
	if textureName != nil {
		*textureName = runtime.CAPIStringViewToString(&textureNameView)
	}
	return res, nil
}

// GetObjectMaterialText wraps CAPI Object_GetMaterialText
func (a *API) GetObjectMaterialText(object *omp.Object, materialIndex int32, text *string, materialSize *int32, fontFace *string, fontSize *int32, bold *bool, fontColor *int32, backgroundColor *int32, textAlignment *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	var textView runtime.CAPIStringView
	var fontFaceView runtime.CAPIStringView
	res := a.capi.Object_GetMaterialText(object.Handle(), materialIndex, &textView, materialSize, &fontFaceView, fontSize, bold, fontColor, backgroundColor, textAlignment)
	if text != nil {
		*text = runtime.CAPIStringViewToString(&textView)
	}
	if fontFace != nil {
		*fontFace = runtime.CAPIStringViewToString(&fontFaceView)
	}
	return res, nil
}

// GetObjectModel wraps CAPI Object_GetModel
func (a *API) GetObjectModel(object *omp.Object) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidObject
	}
	res := a.capi.Object_GetModel(object.Handle())
	return res, nil
}

// GetObjectMoveSpeed wraps CAPI Object_GetMoveSpeed
func (a *API) GetObjectMoveSpeed(object *omp.Object) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidObject
	}
	res := a.capi.Object_GetMoveSpeed(object.Handle())
	return res, nil
}

// GetObjectMovingTargetPos wraps CAPI Object_GetMovingTargetPos
func (a *API) GetObjectMovingTargetPos(object *omp.Object, targetX *float32, targetY *float32, targetZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_GetMovingTargetPos(object.Handle(), targetX, targetY, targetZ)
	return res, nil
}

// GetObjectMovingTargetRot wraps CAPI Object_GetMovingTargetRot
func (a *API) GetObjectMovingTargetRot(object *omp.Object, rotationX *float32, rotationY *float32, rotationZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_GetMovingTargetRot(object.Handle(), rotationX, rotationY, rotationZ)
	return res, nil
}

// GetObjectPos wraps CAPI Object_GetPos
func (a *API) GetObjectPos(object *omp.Object, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_GetPos(object.Handle(), x, y, z)
	return res, nil
}

// GetObjectRot wraps CAPI Object_GetRot
func (a *API) GetObjectRot(object *omp.Object, rotationX *float32, rotationY *float32, rotationZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_GetRot(object.Handle(), rotationX, rotationY, rotationZ)
	return res, nil
}

// GetObjectSyncRotation wraps CAPI Object_GetSyncRotation
func (a *API) GetObjectSyncRotation(object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_GetSyncRotation(object.Handle())
	return res, nil
}

// GetObjectType wraps CAPI Object_GetType
func (a *API) GetObjectType(player *omp.Player, objectid int32) (uint8, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Object_GetType(player.Handle(), objectid)
	return res, nil
}

// GetOccupant wraps CAPI Vehicle_GetOccupant
func (a *API) GetOccupant(vehicle *omp.Vehicle, seat int32) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return nil, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetOccupant(vehicle.Handle(), seat)
	return omp.NewVehicle(res), nil
}

// GetOccupiedTick wraps CAPI Vehicle_GetOccupiedTick
func (a *API) GetOccupiedTick(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetOccupiedTick(vehicle.Handle())
	return res, nil
}

// GetPaintjob wraps CAPI Vehicle_GetPaintjob
func (a *API) GetPaintjob(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetPaintjob(vehicle.Handle())
	return res, nil
}

// GetParamsCarDoors wraps CAPI Vehicle_GetParamsCarDoors
func (a *API) GetParamsCarDoors(vehicle *omp.Vehicle, frontLeft *int32, frontRight *int32, rearLeft *int32, rearRight *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetParamsCarDoors(vehicle.Handle(), frontLeft, frontRight, rearLeft, rearRight)
	return res, nil
}

// GetParamsCarWindows wraps CAPI Vehicle_GetParamsCarWindows
func (a *API) GetParamsCarWindows(vehicle *omp.Vehicle, frontLeft *int32, frontRight *int32, rearLeft *int32, rearRight *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetParamsCarWindows(vehicle.Handle(), frontLeft, frontRight, rearLeft, rearRight)
	return res, nil
}

// GetParamsEx wraps CAPI Vehicle_GetParamsEx
func (a *API) GetParamsEx(vehicle *omp.Vehicle, engine *int32, lights *int32, alarm *int32, doors *int32, bonnet *int32, boot *int32, objective *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetParamsEx(vehicle.Handle(), engine, lights, alarm, doors, bonnet, boot, objective)
	return res, nil
}

// GetParamsSirenState wraps CAPI Vehicle_GetParamsSirenState
func (a *API) GetParamsSirenState(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetParamsSirenState(vehicle.Handle())
	return res, nil
}

// GetPickupID wraps CAPI Pickup_GetID
func (a *API) GetPickupID(pickup *omp.Pickup) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return 0, ErrInvalidPickup
	}
	res := a.capi.Pickup_GetID(pickup.Handle())
	return res, nil
}

// GetPickupModel wraps CAPI Pickup_GetModel
func (a *API) GetPickupModel(pickup *omp.Pickup) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return 0, ErrInvalidPickup
	}
	res := a.capi.Pickup_GetModel(pickup.Handle())
	return res, nil
}

// GetPickupPos wraps CAPI Pickup_GetPos
func (a *API) GetPickupPos(pickup *omp.Pickup, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_GetPos(pickup.Handle(), x, y, z)
	return res, nil
}

// GetPickupType wraps CAPI Pickup_GetType
func (a *API) GetPickupType(pickup *omp.Pickup) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return 0, ErrInvalidPickup
	}
	res := a.capi.Pickup_GetType(pickup.Handle())
	return res, nil
}

// GetPickupVirtualWorld wraps CAPI Pickup_GetVirtualWorld
func (a *API) GetPickupVirtualWorld(pickup *omp.Pickup) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return 0, ErrInvalidPickup
	}
	res := a.capi.Pickup_GetVirtualWorld(pickup.Handle())
	return res, nil
}

// GetPing wraps CAPI Player_GetPing
func (a *API) GetPing(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetPing(player.Handle())
	return res, nil
}

// GetPlayerAmmo wraps CAPI Player_GetPlayerAmmo
func (a *API) GetPlayerAmmo(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetPlayerAmmo(player.Handle())
	return res, nil
}

// GetPlayerColor wraps CAPI Player_GetColor
func (a *API) GetPlayerColor(player *omp.Player) (uint32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetColor(player.Handle())
	return res, nil
}

// GetPlayerDistanceFromPoint wraps CAPI Player_GetDistanceFromPoint
func (a *API) GetPlayerDistanceFromPoint(player *omp.Player, x float32, y float32, z float32) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetDistanceFromPoint(player.Handle(), x, y, z)
	return res, nil
}

// GetPlayerFacingAngle wraps CAPI Player_GetFacingAngle
func (a *API) GetPlayerFacingAngle(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetFacingAngle(player.Handle())
	return res, nil
}

// GetPlayerGravity wraps CAPI Player_GetGravity
func (a *API) GetPlayerGravity(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetGravity(player.Handle())
	return res, nil
}

// GetPlayerHealth wraps CAPI Player_GetHealth
func (a *API) GetPlayerHealth(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetHealth(player.Handle())
	return res, nil
}

// GetPlayerHydraReactorAngle wraps CAPI Player_GetHydraReactorAngle
func (a *API) GetPlayerHydraReactorAngle(player *omp.Player) (uint32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetHydraReactorAngle(player.Handle())
	return res, nil
}

// GetPlayerID wraps CAPI Player_GetID
func (a *API) GetPlayerID(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetID(player.Handle())
	return res, nil
}

// GetPlayerInterior wraps CAPI Player_GetInterior
func (a *API) GetPlayerInterior(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetInterior(player.Handle())
	return res, nil
}

// GetPlayerLandingGearState wraps CAPI Player_GetLandingGearState
func (a *API) GetPlayerLandingGearState(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetLandingGearState(player.Handle())
	return res, nil
}

// GetPlayerObjectAttachedData wraps CAPI PlayerObject_GetAttachedData
func (a *API) GetPlayerObjectAttachedData(player *omp.Player, object *omp.PlayerObject, parentVehicle *int32, parentObject *int32, parentPlayer *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetAttachedData(player.Handle(), object.Handle(), parentVehicle, parentObject, parentPlayer)
	return res, nil
}

// GetPlayerObjectAttachedOffset wraps CAPI PlayerObject_GetAttachedOffset
func (a *API) GetPlayerObjectAttachedOffset(player *omp.Player, object *omp.PlayerObject, offsetX *float32, offsetY *float32, offsetZ *float32, rotationX *float32, rotationY *float32, rotationZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetAttachedOffset(player.Handle(), object.Handle(), offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ)
	return res, nil
}

// GetPlayerObjectDrawDistance wraps CAPI PlayerObject_GetDrawDistance
func (a *API) GetPlayerObjectDrawDistance(player *omp.Player, object *omp.PlayerObject) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetDrawDistance(player.Handle(), object.Handle())
	return res, nil
}

// GetPlayerObjectID wraps CAPI PlayerObject_GetID
func (a *API) GetPlayerObjectID(player *omp.Player, object *omp.PlayerObject) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetID(player.Handle(), object.Handle())
	return res, nil
}

// GetPlayerObjectMaterial wraps CAPI PlayerObject_GetMaterial
func (a *API) GetPlayerObjectMaterial(player *omp.Player, object *omp.PlayerObject, materialIndex int32, modelid *int32, textureLibrary *string, textureName *string, materialColor *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	var textureLibraryView runtime.CAPIStringView
	var textureNameView runtime.CAPIStringView
	res := a.capi.PlayerObject_GetMaterial(player.Handle(), object.Handle(), materialIndex, modelid, &textureLibraryView, &textureNameView, materialColor)
	if textureLibrary != nil {
		*textureLibrary = runtime.CAPIStringViewToString(&textureLibraryView)
	}
	if textureName != nil {
		*textureName = runtime.CAPIStringViewToString(&textureNameView)
	}
	return res, nil
}

// GetPlayerObjectMaterialText wraps CAPI PlayerObject_GetMaterialText
func (a *API) GetPlayerObjectMaterialText(player *omp.Player, object *omp.PlayerObject, materialIndex int32, text *string, materialSize *int32, fontFace *string, fontSize *int32, bold *bool, fontColor *int32, backgroundColor *int32, textAlignment *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	var textView runtime.CAPIStringView
	var fontFaceView runtime.CAPIStringView
	res := a.capi.PlayerObject_GetMaterialText(player.Handle(), object.Handle(), materialIndex, &textView, materialSize, &fontFaceView, fontSize, bold, fontColor, backgroundColor, textAlignment)
	if text != nil {
		*text = runtime.CAPIStringViewToString(&textView)
	}
	if fontFace != nil {
		*fontFace = runtime.CAPIStringViewToString(&fontFaceView)
	}
	return res, nil
}

// GetPlayerObjectModel wraps CAPI PlayerObject_GetModel
func (a *API) GetPlayerObjectModel(player *omp.Player, object *omp.PlayerObject) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetModel(player.Handle(), object.Handle())
	return res, nil
}

// GetPlayerObjectMoveSpeed wraps CAPI PlayerObject_GetMoveSpeed
func (a *API) GetPlayerObjectMoveSpeed(player *omp.Player, object *omp.PlayerObject) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetMoveSpeed(player.Handle(), object.Handle())
	return res, nil
}

// GetPlayerObjectMovingTargetPos wraps CAPI PlayerObject_GetMovingTargetPos
func (a *API) GetPlayerObjectMovingTargetPos(player *omp.Player, object *omp.PlayerObject, targetX *float32, targetY *float32, targetZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetMovingTargetPos(player.Handle(), object.Handle(), targetX, targetY, targetZ)
	return res, nil
}

// GetPlayerObjectMovingTargetRot wraps CAPI PlayerObject_GetMovingTargetRot
func (a *API) GetPlayerObjectMovingTargetRot(player *omp.Player, object *omp.PlayerObject, rotationX *float32, rotationY *float32, rotationZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetMovingTargetRot(player.Handle(), object.Handle(), rotationX, rotationY, rotationZ)
	return res, nil
}

// GetPlayerObjectPos wraps CAPI PlayerObject_GetPos
func (a *API) GetPlayerObjectPos(player *omp.Player, object *omp.PlayerObject, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetPos(player.Handle(), object.Handle(), x, y, z)
	return res, nil
}

// GetPlayerObjectRot wraps CAPI PlayerObject_GetRot
func (a *API) GetPlayerObjectRot(player *omp.Player, object *omp.PlayerObject, rotationX *float32, rotationY *float32, rotationZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetRot(player.Handle(), object.Handle(), rotationX, rotationY, rotationZ)
	return res, nil
}

// GetPlayerObjectSyncRotation wraps CAPI PlayerObject_GetSyncRotation
func (a *API) GetPlayerObjectSyncRotation(player *omp.Player, object *omp.PlayerObject) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_GetSyncRotation(player.Handle(), object.Handle())
	return res, nil
}

// GetPlayerPos wraps CAPI Player_GetPos
func (a *API) GetPlayerPos(player *omp.Player, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetPos(player.Handle(), x, y, z)
	return res, nil
}

// GetPlayerRotationQuat wraps CAPI Player_GetRotationQuat
func (a *API) GetPlayerRotationQuat(player *omp.Player, x *float32, y *float32, z *float32, w *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetRotationQuat(player.Handle(), x, y, z, w)
	return res, nil
}

// GetPlayerSirenState wraps CAPI Player_GetSirenState
func (a *API) GetPlayerSirenState(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSirenState(player.Handle())
	return res, nil
}

// GetPlayerSkin wraps CAPI Player_GetSkin
func (a *API) GetPlayerSkin(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSkin(player.Handle())
	return res, nil
}

// GetPlayerSpawnInfo wraps CAPI Player_GetSpawnInfo
func (a *API) GetPlayerSpawnInfo(player *omp.Player, team *uint8, skin *int32, x *float32, y *float32, z *float32, angle *float32, weapon1 *uint8, ammo1 *uint32, weapon2 *uint8, ammo2 *uint32, weapon3 *uint8, ammo3 *uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSpawnInfo(player.Handle(), team, skin, x, y, z, angle, weapon1, ammo1, weapon2, ammo2, weapon3, ammo3)
	return res, nil
}

// GetPlayerSpectateID wraps CAPI Player_GetPlayerSpectateID
func (a *API) GetPlayerSpectateID(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetPlayerSpectateID(player.Handle())
	return res, nil
}

// GetPlayerTextDrawAlignment wraps CAPI PlayerTextDraw_GetAlignment
func (a *API) GetPlayerTextDrawAlignment(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetAlignment(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawBackgroundColor wraps CAPI PlayerTextDraw_GetBackgroundColor
func (a *API) GetPlayerTextDrawBackgroundColor(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetBackgroundColor(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawBoxColor wraps CAPI PlayerTextDraw_GetBoxColor
func (a *API) GetPlayerTextDrawBoxColor(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetBoxColor(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawColor wraps CAPI PlayerTextDraw_GetColor
func (a *API) GetPlayerTextDrawColor(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetColor(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawFont wraps CAPI PlayerTextDraw_GetFont
func (a *API) GetPlayerTextDrawFont(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetFont(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawID wraps CAPI PlayerTextDraw_GetID
func (a *API) GetPlayerTextDrawID(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetID(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawLetterSize wraps CAPI PlayerTextDraw_GetLetterSize
func (a *API) GetPlayerTextDrawLetterSize(player *omp.Player, textdraw *omp.PlayerTextDraw, x *float32, y *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetLetterSize(player.Handle(), textdraw.Handle(), x, y)
	return res, nil
}

// GetPlayerTextDrawOutline wraps CAPI PlayerTextDraw_GetOutline
func (a *API) GetPlayerTextDrawOutline(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetOutline(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawPos wraps CAPI PlayerTextDraw_GetPos
func (a *API) GetPlayerTextDrawPos(player *omp.Player, textdraw *omp.PlayerTextDraw, x *float32, y *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetPos(player.Handle(), textdraw.Handle(), x, y)
	return res, nil
}

// GetPlayerTextDrawPreviewModel wraps CAPI PlayerTextDraw_GetPreviewModel
func (a *API) GetPlayerTextDrawPreviewModel(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetPreviewModel(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawPreviewRot wraps CAPI PlayerTextDraw_GetPreviewRot
func (a *API) GetPlayerTextDrawPreviewRot(player *omp.Player, textdraw *omp.PlayerTextDraw, rx *float32, ry *float32, rz *float32, zoom *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetPreviewRot(player.Handle(), textdraw.Handle(), rx, ry, rz, zoom)
	return res, nil
}

// GetPlayerTextDrawPreviewVehColor wraps CAPI PlayerTextDraw_GetPreviewVehColor
func (a *API) GetPlayerTextDrawPreviewVehColor(player *omp.Player, textdraw *omp.PlayerTextDraw, color1 *int32, color2 *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetPreviewVehColor(player.Handle(), textdraw.Handle(), color1, color2)
	return res, nil
}

// GetPlayerTextDrawShadow wraps CAPI PlayerTextDraw_GetShadow
func (a *API) GetPlayerTextDrawShadow(player *omp.Player, textdraw *omp.PlayerTextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetShadow(player.Handle(), textdraw.Handle())
	return res, nil
}

// GetPlayerTextDrawString wraps CAPI PlayerTextDraw_GetString
func (a *API) GetPlayerTextDrawString(player *omp.Player, textdraw *omp.PlayerTextDraw) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return "", ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return "", ErrInvalidPlayerTextDraw
	}
	res, ok := a.capi.PlayerTextDraw_GetStringString(player.Handle(), textdraw.Handle())
	if !ok {
		return "", errors.New("gamemode: GetPlayerTextDrawString failed")
	}
	return res, nil
}

// GetPlayerTextDrawTextSize wraps CAPI PlayerTextDraw_GetTextSize
func (a *API) GetPlayerTextDrawTextSize(player *omp.Player, textdraw *omp.PlayerTextDraw, x *float32, y *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_GetTextSize(player.Handle(), textdraw.Handle(), x, y)
	return res, nil
}

// GetPlayerTextLabelAttachedData wraps CAPI PlayerTextLabel_GetAttachedData
func (a *API) GetPlayerTextLabelAttachedData(player *omp.Player, textlabel *omp.PlayerTextLabel, attachedPlayer *int32, attachedVehicle *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_GetAttachedData(player.Handle(), textlabel.Handle(), attachedPlayer, attachedVehicle)
	return res, nil
}

// GetPlayerTextLabelColor wraps CAPI PlayerTextLabel_GetColor
func (a *API) GetPlayerTextLabelColor(player *omp.Player, textlabel *omp.PlayerTextLabel, color *uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_GetColor(player.Handle(), textlabel.Handle(), color)
	return res, nil
}

// GetPlayerTextLabelDrawDistance wraps CAPI PlayerTextLabel_GetDrawDistance
func (a *API) GetPlayerTextLabelDrawDistance(player *omp.Player, textlabel *omp.PlayerTextLabel) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return 0, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_GetDrawDistance(player.Handle(), textlabel.Handle())
	return res, nil
}

// GetPlayerTextLabelID wraps CAPI PlayerTextLabel_GetID
func (a *API) GetPlayerTextLabelID(player *omp.Player, textlabel *omp.PlayerTextLabel) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return 0, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_GetID(player.Handle(), textlabel.Handle())
	return res, nil
}

// GetPlayerTextLabelLOS wraps CAPI PlayerTextLabel_GetLOS
func (a *API) GetPlayerTextLabelLOS(player *omp.Player, textlabel *omp.PlayerTextLabel) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_GetLOS(player.Handle(), textlabel.Handle())
	return res, nil
}

// GetPlayerTextLabelPos wraps CAPI PlayerTextLabel_GetPos
func (a *API) GetPlayerTextLabelPos(player *omp.Player, textlabel *omp.PlayerTextLabel, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_GetPos(player.Handle(), textlabel.Handle(), x, y, z)
	return res, nil
}

// GetPlayerTextLabelText wraps CAPI PlayerTextLabel_GetText
func (a *API) GetPlayerTextLabelText(player *omp.Player, textlabel *omp.PlayerTextLabel) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return "", ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return "", ErrInvalidPlayerTextLabel
	}
	res, ok := a.capi.PlayerTextLabel_GetTextString(player.Handle(), textlabel.Handle())
	if !ok {
		return "", errors.New("gamemode: GetPlayerTextLabelText failed")
	}
	return res, nil
}

// GetPlayerTextLabelVirtualWorld wraps CAPI PlayerTextLabel_GetVirtualWorld
func (a *API) GetPlayerTextLabelVirtualWorld(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.PlayerTextLabel_GetVirtualWorld(player.Handle())
	return res, nil
}

// GetPlayerTrainSpeed wraps CAPI Player_GetTrainSpeed
func (a *API) GetPlayerTrainSpeed(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetTrainSpeed(player.Handle())
	return res, nil
}

// GetPlayerVelocity wraps CAPI Player_GetVelocity
func (a *API) GetPlayerVelocity(player *omp.Player, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetVelocity(player.Handle(), x, y, z)
	return res, nil
}

// GetPlayerVirtualWorld wraps CAPI Player_GetVirtualWorld
func (a *API) GetPlayerVirtualWorld(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetVirtualWorld(player.Handle())
	return res, nil
}

// GetPlayerWeather wraps CAPI Player_GetWeather
func (a *API) GetPlayerWeather(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetWeather(player.Handle())
	return res, nil
}

// GetRaceCheckpoint wraps CAPI RaceCheckpoint_Get
func (a *API) GetRaceCheckpoint(player *omp.Player, x *float32, y *float32, z *float32, nextX *float32, nextY *float32, nextZ *float32, radius *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.RaceCheckpoint_Get(player.Handle(), x, y, z, nextX, nextY, nextZ, radius)
	return res, nil
}

// GetRandomColorPair wraps CAPI Vehicle_GetRandomColorPair
func (a *API) GetRandomColorPair(modelid int32, color1 *int32, color2 *int32, color3 *int32, color4 *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Vehicle_GetRandomColorPair(modelid, color1, color2, color3, color4)
	return res, nil
}

// GetRawIP wraps CAPI Player_GetRawIp
func (a *API) GetRawIP(player *omp.Player) (uint32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetRawIp(player.Handle())
	return res, nil
}

// GetRespawnDelay wraps CAPI Vehicle_GetRespawnDelay
func (a *API) GetRespawnDelay(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetRespawnDelay(vehicle.Handle())
	return res, nil
}

// GetRespawnTick wraps CAPI Vehicle_GetRespawnTick
func (a *API) GetRespawnTick(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetRespawnTick(vehicle.Handle())
	return res, nil
}

// GetScore wraps CAPI Player_GetScore
func (a *API) GetScore(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetScore(player.Handle())
	return res, nil
}

// GetSkillLevel wraps CAPI Player_GetSkillLevel
func (a *API) GetSkillLevel(player *omp.Player, skill int32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSkillLevel(player.Handle(), skill)
	return res, nil
}

// GetSpecialAction wraps CAPI Player_GetSpecialAction
func (a *API) GetSpecialAction(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSpecialAction(player.Handle())
	return res, nil
}

// GetSpectateType wraps CAPI Player_GetSpectateType
func (a *API) GetSpectateType(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSpectateType(player.Handle())
	return res, nil
}

// GetState wraps CAPI Player_GetState
func (a *API) GetState(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetState(player.Handle())
	return res, nil
}

// GetSurfingObject wraps CAPI Player_GetSurfingObject
func (a *API) GetSurfingObject(player *omp.Player) (*omp.Object, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSurfingObject(player.Handle())
	return omp.NewObject(res), nil
}

// GetSurfingOffsets wraps CAPI Player_GetSurfingOffsets
func (a *API) GetSurfingOffsets(player *omp.Player, offsetX *float32, offsetY *float32, offsetZ *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSurfingOffsets(player.Handle(), offsetX, offsetY, offsetZ)
	return res, nil
}

// GetSurfingPlayerObject wraps CAPI Player_GetSurfingPlayerObject
func (a *API) GetSurfingPlayerObject(player *omp.Player) (*omp.PlayerObject, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSurfingPlayerObject(player.Handle())
	return omp.NewPlayerObject(res), nil
}

// GetSurfingVehicle wraps CAPI Player_GetSurfingVehicle
func (a *API) GetSurfingVehicle(player *omp.Player) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetSurfingVehicle(player.Handle())
	return omp.NewVehicle(res), nil
}

// GetTargetActor wraps CAPI Player_GetTargetActor
func (a *API) GetTargetActor(player *omp.Player) (*omp.Actor, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetTargetActor(player.Handle())
	return omp.NewActor(res), nil
}

// GetTargetPlayer wraps CAPI Player_GetTargetPlayer
func (a *API) GetTargetPlayer(player *omp.Player) (*omp.Player, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.Player_GetTargetPlayer(player.Handle())
	return omp.NewPlayer(res), nil
}

// GetTeam wraps CAPI Player_GetTeam
func (a *API) GetTeam(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetTeam(player.Handle())
	return res, nil
}

// GetTextDrawAlignment wraps CAPI TextDraw_GetAlignment
func (a *API) GetTextDrawAlignment(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetAlignment(textdraw.Handle())
	return res, nil
}

// GetTextDrawBackgroundColor wraps CAPI TextDraw_GetBackgroundColor
func (a *API) GetTextDrawBackgroundColor(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetBackgroundColor(textdraw.Handle())
	return res, nil
}

// GetTextDrawBoxColor wraps CAPI TextDraw_GetBoxColor
func (a *API) GetTextDrawBoxColor(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetBoxColor(textdraw.Handle())
	return res, nil
}

// GetTextDrawColor wraps CAPI TextDraw_GetColor
func (a *API) GetTextDrawColor(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetColor(textdraw.Handle())
	return res, nil
}

// GetTextDrawFont wraps CAPI TextDraw_GetFont
func (a *API) GetTextDrawFont(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetFont(textdraw.Handle())
	return res, nil
}

// GetTextDrawID wraps CAPI TextDraw_GetID
func (a *API) GetTextDrawID(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetID(textdraw.Handle())
	return res, nil
}

// GetTextDrawLetterSize wraps CAPI TextDraw_GetLetterSize
func (a *API) GetTextDrawLetterSize(textdraw *omp.TextDraw, sizeX *float32, sizeY *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetLetterSize(textdraw.Handle(), sizeX, sizeY)
	return res, nil
}

// GetTextDrawOutline wraps CAPI TextDraw_GetOutline
func (a *API) GetTextDrawOutline(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetOutline(textdraw.Handle())
	return res, nil
}

// GetTextDrawPos wraps CAPI TextDraw_GetPos
func (a *API) GetTextDrawPos(textdraw *omp.TextDraw, x *float32, y *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetPos(textdraw.Handle(), x, y)
	return res, nil
}

// GetTextDrawPreviewModel wraps CAPI TextDraw_GetPreviewModel
func (a *API) GetTextDrawPreviewModel(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetPreviewModel(textdraw.Handle())
	return res, nil
}

// GetTextDrawPreviewRot wraps CAPI TextDraw_GetPreviewRot
func (a *API) GetTextDrawPreviewRot(textdraw *omp.TextDraw, x *float32, y *float32, z *float32, zoom *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetPreviewRot(textdraw.Handle(), x, y, z, zoom)
	return res, nil
}

// GetTextDrawPreviewVehColor wraps CAPI TextDraw_GetPreviewVehColor
func (a *API) GetTextDrawPreviewVehColor(textdraw *omp.TextDraw, color1 *int32, color2 *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetPreviewVehColor(textdraw.Handle(), color1, color2)
	return res, nil
}

// GetTextDrawShadow wraps CAPI TextDraw_GetShadow
func (a *API) GetTextDrawShadow(textdraw *omp.TextDraw) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return 0, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetShadow(textdraw.Handle())
	return res, nil
}

// GetTextDrawString wraps CAPI TextDraw_GetString
func (a *API) GetTextDrawString(textdraw *omp.TextDraw) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return "", ErrInvalidTextDraw
	}
	res, ok := a.capi.TextDraw_GetStringString(textdraw.Handle())
	if !ok {
		return "", errors.New("gamemode: GetTextDrawString failed")
	}
	return res, nil
}

// GetTextDrawTextSize wraps CAPI TextDraw_GetTextSize
func (a *API) GetTextDrawTextSize(textdraw *omp.TextDraw, sizeX *float32, sizeY *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_GetTextSize(textdraw.Handle(), sizeX, sizeY)
	return res, nil
}

// GetTextLabelAttachedData wraps CAPI TextLabel_GetAttachedData
func (a *API) GetTextLabelAttachedData(textlabel *omp.TextLabel, attachedPlayer *int32, attachedVehicle *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_GetAttachedData(textlabel.Handle(), attachedPlayer, attachedVehicle)
	return res, nil
}

// GetTextLabelColor wraps CAPI TextLabel_GetColor
func (a *API) GetTextLabelColor(textlabel *omp.TextLabel) (uint32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return 0, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_GetColor(textlabel.Handle())
	return res, nil
}

// GetTextLabelDrawDistance wraps CAPI TextLabel_GetDrawDistance
func (a *API) GetTextLabelDrawDistance(textlabel *omp.TextLabel) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return 0, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_GetDrawDistance(textlabel.Handle())
	return res, nil
}

// GetTextLabelID wraps CAPI TextLabel_GetID
func (a *API) GetTextLabelID(textlabel *omp.TextLabel) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return 0, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_GetID(textlabel.Handle())
	return res, nil
}

// GetTextLabelLOS wraps CAPI TextLabel_GetLOS
func (a *API) GetTextLabelLOS(textlabel *omp.TextLabel) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_GetLOS(textlabel.Handle())
	return res, nil
}

// GetTextLabelPos wraps CAPI TextLabel_GetPos
func (a *API) GetTextLabelPos(textlabel *omp.TextLabel, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_GetPos(textlabel.Handle(), x, y, z)
	return res, nil
}

// GetTextLabelText wraps CAPI TextLabel_GetText
func (a *API) GetTextLabelText(textlabel *omp.TextLabel) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return "", ErrInvalidTextLabel
	}
	res, ok := a.capi.TextLabel_GetTextString(textlabel.Handle())
	if !ok {
		return "", errors.New("gamemode: GetTextLabelText failed")
	}
	return res, nil
}

// GetTextLabelVirtualWorld wraps CAPI TextLabel_GetVirtualWorld
func (a *API) GetTextLabelVirtualWorld(textlabel *omp.TextLabel) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return 0, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_GetVirtualWorld(textlabel.Handle())
	return res, nil
}

// GetTime wraps CAPI Player_GetTime
func (a *API) GetTime(player *omp.Player, hour *int32, minute *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetTime(player.Handle(), hour, minute)
	return res, nil
}

// GetTower wraps CAPI Vehicle_GetTower
func (a *API) GetTower(vehicle *omp.Vehicle) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return nil, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetTower(vehicle.Handle())
	return omp.NewVehicle(res), nil
}

// GetTrailer wraps CAPI Vehicle_GetTrailer
func (a *API) GetTrailer(vehicle *omp.Vehicle) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return nil, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetTrailer(vehicle.Handle())
	return omp.NewVehicle(res), nil
}

// GetVehicleColor wraps CAPI Vehicle_GetColor
func (a *API) GetVehicleColor(vehicle *omp.Vehicle, color1 *int32, color2 *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetColor(vehicle.Handle(), color1, color2)
	return res, nil
}

// GetVehicleDistanceFromPoint wraps CAPI Vehicle_GetDistanceFromPoint
func (a *API) GetVehicleDistanceFromPoint(vehicle *omp.Vehicle, x float32, y float32, z float32) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetDistanceFromPoint(vehicle.Handle(), x, y, z)
	return res, nil
}

// GetVehicleHealth wraps CAPI Vehicle_GetHealth
func (a *API) GetVehicleHealth(vehicle *omp.Vehicle) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetHealth(vehicle.Handle())
	return res, nil
}

// GetVehicleHydraReactorAngle wraps CAPI Vehicle_GetHydraReactorAngle
func (a *API) GetVehicleHydraReactorAngle(vehicle *omp.Vehicle) (uint32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetHydraReactorAngle(vehicle.Handle())
	return res, nil
}

// GetVehicleID wraps CAPI Player_GetVehicleID
func (a *API) GetVehicleID(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetVehicleID(player.Handle())
	return res, nil
}

// VehicleGetVehicleID wraps CAPI Vehicle_GetID
func (a *API) VehicleGetVehicleID(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetID(vehicle.Handle())
	return res, nil
}

// GetVehicleInterior wraps CAPI Vehicle_GetInterior
func (a *API) GetVehicleInterior(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetInterior(vehicle.Handle())
	return res, nil
}

// GetVehicleLandingGearState wraps CAPI Vehicle_GetLandingGearState
func (a *API) GetVehicleLandingGearState(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetLandingGearState(vehicle.Handle())
	return res, nil
}

// GetVehicleModel wraps CAPI Vehicle_GetModel
func (a *API) GetVehicleModel(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetModel(vehicle.Handle())
	return res, nil
}

// GetVehiclePos wraps CAPI Vehicle_GetPos
func (a *API) GetVehiclePos(vehicle *omp.Vehicle, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetPos(vehicle.Handle(), x, y, z)
	return res, nil
}

// GetVehicleRotationQuat wraps CAPI Vehicle_GetRotationQuat
func (a *API) GetVehicleRotationQuat(vehicle *omp.Vehicle, w *float32, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetRotationQuat(vehicle.Handle(), w, x, y, z)
	return res, nil
}

// GetVehicleSeat wraps CAPI Player_GetVehicleSeat
func (a *API) GetVehicleSeat(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetVehicleSeat(player.Handle())
	return res, nil
}

// GetVehicleSirenState wraps CAPI Vehicle_GetSirenState
func (a *API) GetVehicleSirenState(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetSirenState(vehicle.Handle())
	return res, nil
}

// GetVehicleSpawnInfo wraps CAPI Vehicle_GetSpawnInfo
func (a *API) GetVehicleSpawnInfo(vehicle *omp.Vehicle, x *float32, y *float32, z *float32, rotation *float32, color1 *int32, color2 *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetSpawnInfo(vehicle.Handle(), x, y, z, rotation, color1, color2)
	return res, nil
}

// GetVehicleTrainSpeed wraps CAPI Vehicle_GetTrainSpeed
func (a *API) GetVehicleTrainSpeed(vehicle *omp.Vehicle) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetTrainSpeed(vehicle.Handle())
	return res, nil
}

// GetVehicleVelocity wraps CAPI Vehicle_GetVelocity
func (a *API) GetVehicleVelocity(vehicle *omp.Vehicle, x *float32, y *float32, z *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetVelocity(vehicle.Handle(), x, y, z)
	return res, nil
}

// GetVehicleVirtualWorld wraps CAPI Vehicle_GetVirtualWorld
func (a *API) GetVehicleVirtualWorld(vehicle *omp.Vehicle) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetVirtualWorld(vehicle.Handle())
	return res, nil
}

// GetVersion wraps CAPI Player_GetVersion
func (a *API) GetVersion(player *omp.Player) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return "", ErrInvalidPlayer
	}
	res, ok := a.capi.Player_GetVersionString(player.Handle())
	if !ok {
		return "", errors.New("gamemode: GetVersion failed")
	}
	return res, nil
}

// GetWantedLevel wraps CAPI Player_GetWantedLevel
func (a *API) GetWantedLevel(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetWantedLevel(player.Handle())
	return res, nil
}

// GetWeapon wraps CAPI Player_GetWeapon
func (a *API) GetWeapon(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetWeapon(player.Handle())
	return res, nil
}

// GetWeaponData wraps CAPI Player_GetWeaponData
func (a *API) GetWeaponData(player *omp.Player, slot int32, weaponid *int32, ammo *int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetWeaponData(player.Handle(), slot, weaponid, ammo)
	return res, nil
}

// GetWeaponName wraps CAPI Core_GetWeaponName
func (a *API) GetWeaponName(weaponid int32) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	res, ok := a.capi.Core_GetWeaponNameString(weaponid)
	if !ok {
		return "", errors.New("gamemode: GetWeaponName failed")
	}
	return res, nil
}

// GetWeaponSlot wraps CAPI Core_GetWeaponSlot
func (a *API) GetWeaponSlot(weapon uint8) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Core_GetWeaponSlot(weapon)
	return res, nil
}

// GetWeaponState wraps CAPI Player_GetWeaponState
func (a *API) GetWeaponState(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetWeaponState(player.Handle())
	return res, nil
}

// GetWorldBounds wraps CAPI Player_GetWorldBounds
func (a *API) GetWorldBounds(player *omp.Player, xmax *float32, xmin *float32, ymax *float32, ymin *float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GetWorldBounds(player.Handle(), xmax, xmin, ymax, ymin)
	return res, nil
}

// GetWorldTime wraps CAPI Core_GetWorldTime
func (a *API) GetWorldTime() (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Core_GetWorldTime()
	return res, nil
}

// GetZAim wraps CAPI Player_GetZAim
func (a *API) GetZAim(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_GetZAim(player.Handle())
	return res, nil
}

// GetZAngle wraps CAPI Vehicle_GetZAngle
func (a *API) GetZAngle(vehicle *omp.Vehicle) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return 0, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_GetZAngle(vehicle.Handle())
	return res, nil
}

// GiveMoney wraps CAPI Player_GiveMoney
func (a *API) GiveMoney(player *omp.Player, amount int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GiveMoney(player.Handle(), amount)
	return res, nil
}

// GiveWeapon wraps CAPI Player_GiveWeapon
func (a *API) GiveWeapon(player *omp.Player, weapon int32, ammo int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_GiveWeapon(player.Handle(), weapon, ammo)
	return res, nil
}

// HasBeenOccupied wraps CAPI Vehicle_HasBeenOccupied
func (a *API) HasBeenOccupied(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_HasBeenOccupied(vehicle.Handle())
	return res, nil
}

// HasClock wraps CAPI Player_HasClock
func (a *API) HasClock(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_HasClock(player.Handle())
	return res, nil
}

// HasGameText wraps CAPI Player_HasGameText
func (a *API) HasGameText(player *omp.Player, style int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_HasGameText(player.Handle(), style)
	return res, nil
}

// HideDialog wraps CAPI Dialog_Hide
func (a *API) HideDialog(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Dialog_Hide(player.Handle())
	return res, nil
}

// HideGameText wraps CAPI Player_HideGameText
func (a *API) HideGameText(player *omp.Player, style int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_HideGameText(player.Handle(), style)
	return res, nil
}

// HideGameTextForAll wraps CAPI Core_HideGameTextForAll
func (a *API) HideGameTextForAll(style int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_HideGameTextForAll(style)
	return res, nil
}

// HideGangZoneForAll wraps CAPI GangZone_HideForAll
func (a *API) HideGangZoneForAll(gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_HideForAll(gangzone.Handle())
	return res, nil
}

// HideGangZoneForPlayer wraps CAPI GangZone_HideForPlayer
func (a *API) HideGangZoneForPlayer(player *omp.Player, gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_HideForPlayer(player.Handle(), gangzone.Handle())
	return res, nil
}

// HideMenuForPlayer wraps CAPI Menu_HideForPlayer
func (a *API) HideMenuForPlayer(menu *omp.Menu, player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Menu_HideForPlayer(menu.Handle(), player.Handle())
	return res, nil
}

// HidePickupForPlayer wraps CAPI Pickup_HideForPlayer
func (a *API) HidePickupForPlayer(player *omp.Player, pickup *omp.Pickup) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_HideForPlayer(player.Handle(), pickup.Handle())
	return res, nil
}

// HidePlayerTextDraw wraps CAPI PlayerTextDraw_Hide
func (a *API) HidePlayerTextDraw(player *omp.Player, textdraw *omp.PlayerTextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_Hide(player.Handle(), textdraw.Handle())
	return res, nil
}

// HideTextDrawForAll wraps CAPI TextDraw_HideForAll
func (a *API) HideTextDrawForAll(textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_HideForAll(textdraw.Handle())
	return res, nil
}

// HideTextDrawForPlayer wraps CAPI TextDraw_HideForPlayer
func (a *API) HideTextDrawForPlayer(player *omp.Player, textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_HideForPlayer(player.Handle(), textdraw.Handle())
	return res, nil
}

// InterpolateCameraLookAt wraps CAPI Player_InterpolateCameraLookAt
func (a *API) InterpolateCameraLookAt(player *omp.Player, fromX float32, fromY float32, fromZ float32, toX float32, toY float32, toZ float32, time int32, cut int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_InterpolateCameraLookAt(player.Handle(), fromX, fromY, fromZ, toX, toY, toZ, time, cut)
	return res, nil
}

// InterpolateCameraPos wraps CAPI Player_InterpolateCameraPos
func (a *API) InterpolateCameraPos(player *omp.Player, fromX float32, fromY float32, fromZ float32, toX float32, toY float32, toZ float32, time int32, cut int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_InterpolateCameraPos(player.Handle(), fromX, fromY, fromZ, toX, toY, toZ, time, cut)
	return res, nil
}

// IsActorValid wraps CAPI Actor_IsValid
func (a *API) IsActorValid(actor *omp.Actor) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_IsValid(actor.Handle())
	return res, nil
}

// IsAdmin wraps CAPI Player_IsAdmin
func (a *API) IsAdmin(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsAdmin(player.Handle())
	return res, nil
}

// IsAdminTeleportAllowed wraps CAPI Core_IsAdminTeleportAllowed
func (a *API) IsAdminTeleportAllowed() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_IsAdminTeleportAllowed()
	return res, nil
}

// IsAnimationLibraryValid wraps CAPI Core_IsAnimationLibraryValid
func (a *API) IsAnimationLibraryValid(name string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_IsAnimationLibraryValid(name)
	return res, nil
}

// IsCameraTargetEnabled wraps CAPI Player_IsCameraTargetEnabled
func (a *API) IsCameraTargetEnabled(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsCameraTargetEnabled(player.Handle())
	return res, nil
}

// IsChatTextReplacementToggled wraps CAPI Core_IsChatTextReplacementToggled
func (a *API) IsChatTextReplacementToggled() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_IsChatTextReplacementToggled()
	return res, nil
}

// IsCheckpointActive wraps CAPI Checkpoint_IsActive
func (a *API) IsCheckpointActive(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Checkpoint_IsActive(player.Handle())
	return res, nil
}

// IsCheckpointPlayerIn wraps CAPI Checkpoint_IsPlayerIn
func (a *API) IsCheckpointPlayerIn(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Checkpoint_IsPlayerIn(player.Handle())
	return res, nil
}

// IsControllable wraps CAPI Player_IsControllable
func (a *API) IsControllable(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsControllable(player.Handle())
	return res, nil
}

// IsCuffed wraps CAPI Player_IsCuffed
func (a *API) IsCuffed(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsCuffed(player.Handle())
	return res, nil
}

// IsDead wraps CAPI Vehicle_IsDead
func (a *API) IsDead(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_IsDead(vehicle.Handle())
	return res, nil
}

// IsDisabled wraps CAPI Menu_IsDisabled
func (a *API) IsDisabled(menu *omp.Menu) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_IsDisabled(menu.Handle())
	return res, nil
}

// IsFlashingForPlayer wraps CAPI GangZone_IsFlashingForPlayer
func (a *API) IsFlashingForPlayer(player *omp.Player, gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_IsFlashingForPlayer(player.Handle(), gangzone.Handle())
	return res, nil
}

// IsGangZonePlayerIn wraps CAPI GangZone_IsPlayerIn
func (a *API) IsGangZonePlayerIn(player *omp.Player, gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_IsPlayerIn(player.Handle(), gangzone.Handle())
	return res, nil
}

// IsGangZoneValid wraps CAPI GangZone_IsValid
func (a *API) IsGangZoneValid(gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_IsValid(gangzone.Handle())
	return res, nil
}

// IsGangZoneVisibleForPlayer wraps CAPI GangZone_IsVisibleForPlayer
func (a *API) IsGangZoneVisibleForPlayer(player *omp.Player, gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_IsVisibleForPlayer(player.Handle(), gangzone.Handle())
	return res, nil
}

// IsHiddenForPlayer wraps CAPI Pickup_IsHiddenForPlayer
func (a *API) IsHiddenForPlayer(player *omp.Player, pickup *omp.Pickup) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_IsHiddenForPlayer(player.Handle(), pickup.Handle())
	return res, nil
}

// IsIPAddressBanned wraps CAPI Core_IsIpAddressBanned
func (a *API) IsIPAddressBanned(ip string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_IsIpAddressBanned(ip)
	return res, nil
}

// IsInAnyVehicle wraps CAPI Player_IsInAnyVehicle
func (a *API) IsInAnyVehicle(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsInAnyVehicle(player.Handle())
	return res, nil
}

// IsInDriveByMode wraps CAPI Player_IsInDriveByMode
func (a *API) IsInDriveByMode(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsInDriveByMode(player.Handle())
	return res, nil
}

// IsInModShop wraps CAPI Player_IsInModShop
func (a *API) IsInModShop(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsInModShop(player.Handle())
	return res, nil
}

// IsInRangeOfPoint wraps CAPI Player_IsInRangeOfPoint
func (a *API) IsInRangeOfPoint(player *omp.Player, rng float32, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsInRangeOfPoint(player.Handle(), rng, x, y, z)
	return res, nil
}

// IsInVehicle wraps CAPI Player_IsInVehicle
func (a *API) IsInVehicle(player *omp.Player, targetVehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if targetVehicle == nil || !targetVehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Player_IsInVehicle(player.Handle(), targetVehicle.Handle())
	return res, nil
}

// IsInvulnerable wraps CAPI Actor_IsInvulnerable
func (a *API) IsInvulnerable(actor *omp.Actor) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_IsInvulnerable(actor.Handle())
	return res, nil
}

// IsMenuValid wraps CAPI Menu_IsValid
func (a *API) IsMenuValid(menu *omp.Menu) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_IsValid(menu.Handle())
	return res, nil
}

// IsNPC wraps CAPI Player_IsNPC
func (a *API) IsNPC(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsNPC(player.Handle())
	return res, nil
}

// IsNickNameCharacterAllowed wraps CAPI Core_IsNickNameCharacterAllowed
func (a *API) IsNickNameCharacterAllowed(character int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_IsNickNameCharacterAllowed(character)
	return res, nil
}

// IsNickNameValid wraps CAPI Core_IsNickNameValid
func (a *API) IsNickNameValid(name string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_IsNickNameValid(name)
	return res, nil
}

// IsNoCameraCollision wraps CAPI PlayerObject_IsNoCameraCollision
func (a *API) IsNoCameraCollision(player *omp.Player, object *omp.PlayerObject) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_IsNoCameraCollision(player.Handle(), object.Handle())
	return res, nil
}

// IsObjectMaterialSlotUsed wraps CAPI Object_IsMaterialSlotUsed
func (a *API) IsObjectMaterialSlotUsed(object *omp.Object, materialIndex int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_IsMaterialSlotUsed(object.Handle(), materialIndex)
	return res, nil
}

// IsObjectMoving wraps CAPI Object_IsMoving
func (a *API) IsObjectMoving(object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_IsMoving(object.Handle())
	return res, nil
}

// IsObjectNoCameraCollision wraps CAPI Object_IsObjectNoCameraCollision
func (a *API) IsObjectNoCameraCollision(object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_IsObjectNoCameraCollision(object.Handle())
	return res, nil
}

// IsObjectValid wraps CAPI Object_IsValid
func (a *API) IsObjectValid(object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_IsValid(object.Handle())
	return res, nil
}

// IsOccupied wraps CAPI Vehicle_IsOccupied
func (a *API) IsOccupied(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_IsOccupied(vehicle.Handle())
	return res, nil
}

// IsPickupStreamedIn wraps CAPI Pickup_IsStreamedIn
func (a *API) IsPickupStreamedIn(player *omp.Player, pickup *omp.Pickup) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_IsStreamedIn(player.Handle(), pickup.Handle())
	return res, nil
}

// IsPickupValid wraps CAPI Pickup_IsValid
func (a *API) IsPickupValid(pickup *omp.Pickup) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_IsValid(pickup.Handle())
	return res, nil
}

// IsPlayerAttachedObjectSlotUsed wraps CAPI Player_IsPlayerAttachedObjectSlotUsed
func (a *API) IsPlayerAttachedObjectSlotUsed(player *omp.Player, index int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsPlayerAttachedObjectSlotUsed(player.Handle(), index)
	return res, nil
}

// IsPlayerObjectMaterialSlotUsed wraps CAPI PlayerObject_IsMaterialSlotUsed
func (a *API) IsPlayerObjectMaterialSlotUsed(player *omp.Player, object *omp.PlayerObject, materialIndex int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_IsMaterialSlotUsed(player.Handle(), object.Handle(), materialIndex)
	return res, nil
}

// IsPlayerObjectMoving wraps CAPI PlayerObject_IsMoving
func (a *API) IsPlayerObjectMoving(player *omp.Player, object *omp.PlayerObject) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_IsMoving(player.Handle(), object.Handle())
	return res, nil
}

// IsPlayerObjectValid wraps CAPI PlayerObject_IsValid
func (a *API) IsPlayerObjectValid(player *omp.Player, object *omp.PlayerObject) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_IsValid(player.Handle(), object.Handle())
	return res, nil
}

// IsPlayerStreamedIn wraps CAPI Player_IsStreamedIn
func (a *API) IsPlayerStreamedIn(player *omp.Player, other *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if other == nil || !other.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsStreamedIn(player.Handle(), other.Handle())
	return res, nil
}

// IsPlayerTextDrawBox wraps CAPI PlayerTextDraw_IsBox
func (a *API) IsPlayerTextDrawBox(player *omp.Player, textdraw *omp.PlayerTextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_IsBox(player.Handle(), textdraw.Handle())
	return res, nil
}

// IsPlayerTextDrawProportional wraps CAPI PlayerTextDraw_IsProportional
func (a *API) IsPlayerTextDrawProportional(player *omp.Player, textdraw *omp.PlayerTextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_IsProportional(player.Handle(), textdraw.Handle())
	return res, nil
}

// IsPlayerTextDrawSelectable wraps CAPI PlayerTextDraw_IsSelectable
func (a *API) IsPlayerTextDrawSelectable(player *omp.Player, textdraw *omp.PlayerTextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_IsSelectable(player.Handle(), textdraw.Handle())
	return res, nil
}

// IsPlayerTextDrawValid wraps CAPI PlayerTextDraw_IsValid
func (a *API) IsPlayerTextDrawValid(player *omp.Player, textdraw *omp.PlayerTextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_IsValid(player.Handle(), textdraw.Handle())
	return res, nil
}

// IsPlayerTextLabelValid wraps CAPI PlayerTextLabel_IsValid
func (a *API) IsPlayerTextLabelValid(player *omp.Player, textlabel *omp.PlayerTextLabel, valid *bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_IsValid(player.Handle(), textlabel.Handle(), valid)
	return res, nil
}

// IsPlayerUsingOfficialClient wraps CAPI Player_IsPlayerUsingOfficialClient
func (a *API) IsPlayerUsingOfficialClient(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsPlayerUsingOfficialClient(player.Handle())
	return res, nil
}

// IsRaceCheckpointActive wraps CAPI RaceCheckpoint_IsActive
func (a *API) IsRaceCheckpointActive(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.RaceCheckpoint_IsActive(player.Handle())
	return res, nil
}

// IsRaceCheckpointPlayerIn wraps CAPI RaceCheckpoint_IsPlayerIn
func (a *API) IsRaceCheckpointPlayerIn(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.RaceCheckpoint_IsPlayerIn(player.Handle())
	return res, nil
}

// IsRowDisabled wraps CAPI Menu_IsRowDisabled
func (a *API) IsRowDisabled(menu *omp.Menu, row int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_IsRowDisabled(menu.Handle(), row)
	return res, nil
}

// IsSirenEnabled wraps CAPI Vehicle_IsSirenEnabled
func (a *API) IsSirenEnabled(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_IsSirenEnabled(vehicle.Handle())
	return res, nil
}

// IsSpawned wraps CAPI Player_IsSpawned
func (a *API) IsSpawned(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsSpawned(player.Handle())
	return res, nil
}

// IsStreamedInFor wraps CAPI Actor_IsStreamedInFor
func (a *API) IsStreamedInFor(actor *omp.Actor, player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Actor_IsStreamedInFor(actor.Handle(), player.Handle())
	return res, nil
}

// IsTeleportAllowed wraps CAPI Player_IsTeleportAllowed
func (a *API) IsTeleportAllowed(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsTeleportAllowed(player.Handle())
	return res, nil
}

// IsTextDrawBox wraps CAPI TextDraw_IsBox
func (a *API) IsTextDrawBox(textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_IsBox(textdraw.Handle())
	return res, nil
}

// IsTextDrawProportional wraps CAPI TextDraw_IsProportional
func (a *API) IsTextDrawProportional(textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_IsProportional(textdraw.Handle())
	return res, nil
}

// IsTextDrawSelectable wraps CAPI TextDraw_IsSelectable
func (a *API) IsTextDrawSelectable(textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_IsSelectable(textdraw.Handle())
	return res, nil
}

// IsTextDrawValid wraps CAPI TextDraw_IsValid
func (a *API) IsTextDrawValid(textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_IsValid(textdraw.Handle())
	return res, nil
}

// IsTextDrawVisibleForPlayer wraps CAPI TextDraw_IsVisibleForPlayer
func (a *API) IsTextDrawVisibleForPlayer(player *omp.Player, textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_IsVisibleForPlayer(player.Handle(), textdraw.Handle())
	return res, nil
}

// IsTextLabelStreamedIn wraps CAPI TextLabel_IsStreamedIn
func (a *API) IsTextLabelStreamedIn(player *omp.Player, textlabel *omp.TextLabel) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_IsStreamedIn(player.Handle(), textlabel.Handle())
	return res, nil
}

// IsTextLabelValid wraps CAPI TextLabel_IsValid
func (a *API) IsTextLabelValid(textlabel *omp.TextLabel) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_IsValid(textlabel.Handle())
	return res, nil
}

// IsTrailerAttached wraps CAPI Vehicle_IsTrailerAttached
func (a *API) IsTrailerAttached(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_IsTrailerAttached(vehicle.Handle())
	return res, nil
}

// IsUsingOmp wraps CAPI Player_IsUsingOmp
func (a *API) IsUsingOmp(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsUsingOmp(player.Handle())
	return res, nil
}

// IsValidRule wraps CAPI Core_IsValidRule
func (a *API) IsValidRule(name string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_IsValidRule(name)
	return res, nil
}

// IsVehicleStreamedIn wraps CAPI Vehicle_IsStreamedIn
func (a *API) IsVehicleStreamedIn(vehicle *omp.Vehicle, player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Vehicle_IsStreamedIn(vehicle.Handle(), player.Handle())
	return res, nil
}

// IsVehicleValid wraps CAPI Vehicle_IsValid
func (a *API) IsVehicleValid(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_IsValid(vehicle.Handle())
	return res, nil
}

// IsVisible wraps CAPI PlayerTextDraw_IsVisible
func (a *API) IsVisible(player *omp.Player, textdraw *omp.PlayerTextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_IsVisible(player.Handle(), textdraw.Handle())
	return res, nil
}

// IsWidescreenToggled wraps CAPI Player_IsWidescreenToggled
func (a *API) IsWidescreenToggled(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_IsWidescreenToggled(player.Handle())
	return res, nil
}

// Kick wraps CAPI Player_Kick
func (a *API) Kick(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_Kick(player.Handle())
	return res, nil
}

// LinkToInterior wraps CAPI Vehicle_LinkToInterior
func (a *API) LinkToInterior(vehicle *omp.Vehicle, interiorid int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_LinkToInterior(vehicle.Handle(), interiorid)
	return res, nil
}

// CoreLog wraps CAPI Core_Log
func (a *API) CoreLog(text string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_Log(text)
	return res, nil
}

// MaxPlayers wraps CAPI Core_MaxPlayers
func (a *API) MaxPlayers() (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Core_MaxPlayers()
	return res, nil
}

// MenuFromID wraps CAPI Menu_FromID
func (a *API) MenuFromID(menuid int32) (*omp.Menu, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Menu_FromID(menuid)
	return omp.NewMenu(res), nil
}

// NetStatsBytesReceived wraps CAPI Player_NetStatsBytesReceived
func (a *API) NetStatsBytesReceived(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_NetStatsBytesReceived(player.Handle())
	return res, nil
}

// NetStatsBytesSent wraps CAPI Player_NetStatsBytesSent
func (a *API) NetStatsBytesSent(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_NetStatsBytesSent(player.Handle())
	return res, nil
}

// NetStatsConnectionStatus wraps CAPI Player_NetStatsConnectionStatus
func (a *API) NetStatsConnectionStatus(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_NetStatsConnectionStatus(player.Handle())
	return res, nil
}

// NetStatsGetConnectedTime wraps CAPI Player_NetStatsGetConnectedTime
func (a *API) NetStatsGetConnectedTime(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_NetStatsGetConnectedTime(player.Handle())
	return res, nil
}

// NetStatsGetIPPort wraps CAPI Player_NetStatsGetIpPort
func (a *API) NetStatsGetIPPort(player *omp.Player) (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return "", ErrInvalidPlayer
	}
	res, ok := a.capi.Player_NetStatsGetIpPortString(player.Handle())
	if !ok {
		return "", errors.New("gamemode: NetStatsGetIPPort failed")
	}
	return res, nil
}

// NetStatsMessagesReceived wraps CAPI Player_NetStatsMessagesReceived
func (a *API) NetStatsMessagesReceived(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_NetStatsMessagesReceived(player.Handle())
	return res, nil
}

// NetStatsMessagesRecvPerSecond wraps CAPI Player_NetStatsMessagesRecvPerSecond
func (a *API) NetStatsMessagesRecvPerSecond(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_NetStatsMessagesRecvPerSecond(player.Handle())
	return res, nil
}

// NetStatsMessagesSent wraps CAPI Player_NetStatsMessagesSent
func (a *API) NetStatsMessagesSent(player *omp.Player) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_NetStatsMessagesSent(player.Handle())
	return res, nil
}

// NetStatsPacketLossPercent wraps CAPI Player_NetStatsPacketLossPercent
func (a *API) NetStatsPacketLossPercent(player *omp.Player) (float32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_NetStatsPacketLossPercent(player.Handle())
	return res, nil
}

// NetworkStats wraps CAPI Core_NetworkStats
func (a *API) NetworkStats() (string, error) {
	if !a.IsInitialised() {
		return "", ErrNotInitialised
	}
	res, ok := a.capi.Core_NetworkStatsString()
	if !ok {
		return "", errors.New("gamemode: NetworkStats failed")
	}
	return res, nil
}

// ObjectBeginEditing wraps CAPI Object_BeginEditing
func (a *API) ObjectBeginEditing(player *omp.Player, object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_BeginEditing(player.Handle(), object.Handle())
	return res, nil
}

// ObjectFromID wraps CAPI Object_FromID
func (a *API) ObjectFromID(objectid int32) (*omp.Object, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Object_FromID(objectid)
	return omp.NewObject(res), nil
}

// ObjectMove wraps CAPI Object_Move
func (a *API) ObjectMove(object *omp.Object, x float32, y float32, z float32, speed float32, rotationX float32, rotationY float32, rotationZ float32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidObject
	}
	res := a.capi.Object_Move(object.Handle(), x, y, z, speed, rotationX, rotationY, rotationZ)
	return res, nil
}

// PickupFromID wraps CAPI Pickup_FromID
func (a *API) PickupFromID(pickupid int32) (*omp.Pickup, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Pickup_FromID(pickupid)
	return omp.NewPickup(res), nil
}

// PlayAudioStream wraps CAPI Player_PlayAudioStream
func (a *API) PlayAudioStream(player *omp.Player, url string, x float32, y float32, z float32, distance float32, usePos bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_PlayAudioStream(player.Handle(), url, x, y, z, distance, usePos)
	return res, nil
}

// PlayCrimeReport wraps CAPI Player_PlayCrimeReport
func (a *API) PlayCrimeReport(player *omp.Player, suspect *omp.Player, crime int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if suspect == nil || !suspect.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_PlayCrimeReport(player.Handle(), suspect.Handle(), crime)
	return res, nil
}

// PlayGameSound wraps CAPI Player_PlayGameSound
func (a *API) PlayGameSound(player *omp.Player, sound int32, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_PlayGameSound(player.Handle(), sound, x, y, z)
	return res, nil
}

// PlayerFromID wraps CAPI Player_FromID
func (a *API) PlayerFromID(playerid int32) (*omp.Player, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Player_FromID(playerid)
	return omp.NewPlayer(res), nil
}

// PlayerObjectBeginEditing wraps CAPI PlayerObject_BeginEditing
func (a *API) PlayerObjectBeginEditing(player *omp.Player, object *omp.PlayerObject) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_BeginEditing(player.Handle(), object.Handle())
	return res, nil
}

// PlayerObjectFromID wraps CAPI PlayerObject_FromID
func (a *API) PlayerObjectFromID(player *omp.Player, objectid int32) (*omp.PlayerObject, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.PlayerObject_FromID(player.Handle(), objectid)
	return omp.NewPlayerObject(res), nil
}

// PlayerObjectMove wraps CAPI PlayerObject_Move
func (a *API) PlayerObjectMove(player *omp.Player, object *omp.PlayerObject, x float32, y float32, z float32, speed float32, rotationX float32, rotationY float32, rotationZ float32) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return 0, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_Move(player.Handle(), object.Handle(), x, y, z, speed, rotationX, rotationY, rotationZ)
	return res, nil
}

// PlayerTextDrawFromID wraps CAPI PlayerTextDraw_FromID
func (a *API) PlayerTextDrawFromID(player *omp.Player, textdrawid int32) (*omp.PlayerTextDraw, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.PlayerTextDraw_FromID(player.Handle(), textdrawid)
	return omp.NewPlayerTextDraw(res), nil
}

// PlayerTextLabelFromID wraps CAPI PlayerTextLabel_FromID
func (a *API) PlayerTextLabelFromID(player *omp.Player, textlabelid int32) (*omp.PlayerTextLabel, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return nil, ErrInvalidPlayer
	}
	res := a.capi.PlayerTextLabel_FromID(player.Handle(), textlabelid)
	return omp.NewPlayerTextLabel(res), nil
}

// PutInVehicle wraps CAPI Player_PutInVehicle
func (a *API) PutInVehicle(player *omp.Player, vehicle *omp.Vehicle, seat int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Player_PutInVehicle(player.Handle(), vehicle.Handle(), seat)
	return res, nil
}

// RemoveAttachedObject wraps CAPI Player_RemoveAttachedObject
func (a *API) RemoveAttachedObject(player *omp.Player, index int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_RemoveAttachedObject(player.Handle(), index)
	return res, nil
}

// RemoveBuilding wraps CAPI Player_RemoveBuilding
func (a *API) RemoveBuilding(player *omp.Player, model int32, x float32, y float32, z float32, radius float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_RemoveBuilding(player.Handle(), model, x, y, z, radius)
	return res, nil
}

// RemoveComponent wraps CAPI Vehicle_RemoveComponent
func (a *API) RemoveComponent(vehicle *omp.Vehicle, componentid int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_RemoveComponent(vehicle.Handle(), componentid)
	return res, nil
}

// RemoveFromVehicle wraps CAPI Player_RemoveFromVehicle
func (a *API) RemoveFromVehicle(player *omp.Player, force bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_RemoveFromVehicle(player.Handle(), force)
	return res, nil
}

// RemoveMapIcon wraps CAPI Player_RemoveMapIcon
func (a *API) RemoveMapIcon(player *omp.Player, icon int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_RemoveMapIcon(player.Handle(), icon)
	return res, nil
}

// RemoveRule wraps CAPI Core_RemoveRule
func (a *API) RemoveRule(name string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_RemoveRule(name)
	return res, nil
}

// RemoveWeapon wraps CAPI Player_RemoveWeapon
func (a *API) RemoveWeapon(player *omp.Player, weapon int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_RemoveWeapon(player.Handle(), weapon)
	return res, nil
}

// Repair wraps CAPI Vehicle_Repair
func (a *API) Repair(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_Repair(vehicle.Handle())
	return res, nil
}

// ResetMoney wraps CAPI Player_ResetMoney
func (a *API) ResetMoney(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ResetMoney(player.Handle())
	return res, nil
}

// ResetWeapons wraps CAPI Player_ResetWeapons
func (a *API) ResetWeapons(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ResetWeapons(player.Handle())
	return res, nil
}

// SelectTextDraw wraps CAPI Player_SelectTextDraw
func (a *API) SelectTextDraw(player *omp.Player, hoverColour uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SelectTextDraw(player.Handle(), hoverColour)
	return res, nil
}

// SendAllClientMessage wraps CAPI All_SendClientMessage
func (a *API) SendAllClientMessage(color uint32, text string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.All_SendClientMessage(color, text)
	return res, nil
}

// SendAllDeathMessage wraps CAPI All_SendDeathMessage
func (a *API) SendAllDeathMessage(killer *omp.Player, killee *omp.Player, weapon int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if killer == nil || !killer.Valid() {
		return false, ErrInvalidPlayer
	}
	if killee == nil || !killee.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.All_SendDeathMessage(killer.Handle(), killee.Handle(), weapon)
	return res, nil
}

// SendClientCheck wraps CAPI Player_SendClientCheck
func (a *API) SendClientCheck(player *omp.Player, actionType int32, address int32, offset int32, count int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SendClientCheck(player.Handle(), actionType, address, offset, count)
	return res, nil
}

// SendMessageToPlayer wraps CAPI Player_SendMessageToPlayer
func (a *API) SendMessageToPlayer(player *omp.Player, sender *omp.Player, message string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if sender == nil || !sender.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SendMessageToPlayer(player.Handle(), sender.Handle(), message)
	return res, nil
}

// SendPlayerClientMessage wraps CAPI Player_SendClientMessage
func (a *API) SendPlayerClientMessage(player *omp.Player, color uint32, text string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SendClientMessage(player.Handle(), color, text)
	return res, nil
}

// SendPlayerDeathMessage wraps CAPI Player_SendDeathMessage
func (a *API) SendPlayerDeathMessage(player *omp.Player, killer *omp.Player, killee *omp.Player, weapon int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if killer == nil || !killer.Valid() {
		return false, ErrInvalidPlayer
	}
	if killee == nil || !killee.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SendDeathMessage(player.Handle(), killer.Handle(), killee.Handle(), weapon)
	return res, nil
}

// SendRconCommand wraps CAPI Core_SendRconCommand
func (a *API) SendRconCommand(command string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_SendRconCommand(command)
	return res, nil
}

// ServerTickRate wraps CAPI Core_ServerTickRate
func (a *API) ServerTickRate() (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Core_ServerTickRate()
	return res, nil
}

// SetActorFacingAngle wraps CAPI Actor_SetFacingAngle
func (a *API) SetActorFacingAngle(actor *omp.Actor, angle float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_SetFacingAngle(actor.Handle(), angle)
	return res, nil
}

// SetActorHealth wraps CAPI Actor_SetHealth
func (a *API) SetActorHealth(actor *omp.Actor, hp float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_SetHealth(actor.Handle(), hp)
	return res, nil
}

// SetActorPos wraps CAPI Actor_SetPos
func (a *API) SetActorPos(actor *omp.Actor, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_SetPos(actor.Handle(), x, y, z)
	return res, nil
}

// SetActorSkin wraps CAPI Actor_SetSkin
func (a *API) SetActorSkin(actor *omp.Actor, skin int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_SetSkin(actor.Handle(), skin)
	return res, nil
}

// SetActorVirtualWorld wraps CAPI Actor_SetVirtualWorld
func (a *API) SetActorVirtualWorld(actor *omp.Actor, vw int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_SetVirtualWorld(actor.Handle(), vw)
	return res, nil
}

// SetAdmin wraps CAPI Player_SetAdmin
func (a *API) SetAdmin(player *omp.Player, set bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetAdmin(player.Handle(), set)
	return res, nil
}

// SetAmmo wraps CAPI Player_SetAmmo
func (a *API) SetAmmo(player *omp.Player, id uint8, ammo uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetAmmo(player.Handle(), id, ammo)
	return res, nil
}

// SetAngularVelocity wraps CAPI Vehicle_SetAngularVelocity
func (a *API) SetAngularVelocity(vehicle *omp.Vehicle, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetAngularVelocity(vehicle.Handle(), x, y, z)
	return res, nil
}

// SetArmedWeapon wraps CAPI Player_SetArmedWeapon
func (a *API) SetArmedWeapon(player *omp.Player, weapon uint8) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetArmedWeapon(player.Handle(), weapon)
	return res, nil
}

// SetArmor wraps CAPI Player_SetArmor
func (a *API) SetArmor(player *omp.Player, armor float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetArmor(player.Handle(), armor)
	return res, nil
}

// SetAttachedObject wraps CAPI Player_SetAttachedObject
func (a *API) SetAttachedObject(player *omp.Player, index int32, modelid int32, bone int32, offsetX float32, offsetY float32, offsetZ float32, rotationX float32, rotationY float32, rotationZ float32, scaleX float32, scaleY float32, scaleZ float32, materialcolor1 uint32, materialcolor2 uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetAttachedObject(player.Handle(), index, modelid, bone, offsetX, offsetY, offsetZ, rotationX, rotationY, rotationZ, scaleX, scaleY, scaleZ, materialcolor1, materialcolor2)
	return res, nil
}

// SetCameraBehind wraps CAPI Player_SetCameraBehind
func (a *API) SetCameraBehind(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetCameraBehind(player.Handle())
	return res, nil
}

// SetCameraLookAt wraps CAPI Player_SetCameraLookAt
func (a *API) SetCameraLookAt(player *omp.Player, x float32, y float32, z float32, cutType int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetCameraLookAt(player.Handle(), x, y, z, cutType)
	return res, nil
}

// SetCameraPos wraps CAPI Player_SetCameraPos
func (a *API) SetCameraPos(player *omp.Player, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetCameraPos(player.Handle(), x, y, z)
	return res, nil
}

// SetChatBubble wraps CAPI Player_SetChatBubble
func (a *API) SetChatBubble(player *omp.Player, text string, color uint32, drawdistance float32, expiretime int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetChatBubble(player.Handle(), text, color, drawdistance, expiretime)
	return res, nil
}

// SetChatRadius wraps CAPI Core_SetChatRadius
func (a *API) SetChatRadius(globalChatRadius float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_SetChatRadius(globalChatRadius)
	return res, nil
}

// SetCheckpoint wraps CAPI Checkpoint_Set
func (a *API) SetCheckpoint(player *omp.Player, x float32, y float32, z float32, radius float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Checkpoint_Set(player.Handle(), x, y, z, radius)
	return res, nil
}

// SetColumnHeader wraps CAPI Menu_SetColumnHeader
func (a *API) SetColumnHeader(menu *omp.Menu, column uint8, headerTitle string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	res := a.capi.Menu_SetColumnHeader(menu.Handle(), column, headerTitle)
	return res, nil
}

// SetCoreGravity wraps CAPI Core_SetGravity
func (a *API) SetCoreGravity(gravity float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_SetGravity(gravity)
	return res, nil
}

// SetCoreWeather wraps CAPI Core_SetWeather
func (a *API) SetCoreWeather(weatherid int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_SetWeather(weatherid)
	return res, nil
}

// SetDeathDropAmount wraps CAPI Core_SetDeathDropAmount
func (a *API) SetDeathDropAmount(amount int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_SetDeathDropAmount(amount)
	return res, nil
}

// SetDefaultCameraCollision wraps CAPI Object_SetDefaultCameraCollision
func (a *API) SetDefaultCameraCollision(disable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Object_SetDefaultCameraCollision(disable)
	return res, nil
}

// SetDrunkLevel wraps CAPI Player_SetDrunkLevel
func (a *API) SetDrunkLevel(player *omp.Player, level int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetDrunkLevel(player.Handle(), level)
	return res, nil
}

// SetFightingStyle wraps CAPI Player_SetFightingStyle
func (a *API) SetFightingStyle(player *omp.Player, style int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetFightingStyle(player.Handle(), style)
	return res, nil
}

// SetInterior wraps CAPI Player_SetInterior
func (a *API) SetInterior(player *omp.Player, interior int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetInterior(player.Handle(), interior)
	return res, nil
}

// SetInvulnerable wraps CAPI Actor_SetInvulnerable
func (a *API) SetInvulnerable(actor *omp.Actor, toggle bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if actor == nil || !actor.Valid() {
		return false, ErrInvalidActor
	}
	res := a.capi.Actor_SetInvulnerable(actor.Handle(), toggle)
	return res, nil
}

// SetMapIcon wraps CAPI Player_SetMapIcon
func (a *API) SetMapIcon(player *omp.Player, iconID int32, x float32, y float32, z float32, typ int32, color uint32, style int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetMapIcon(player.Handle(), iconID, x, y, z, typ, color, style)
	return res, nil
}

// SetMarkerForPlayer wraps CAPI Player_SetMarkerForPlayer
func (a *API) SetMarkerForPlayer(player *omp.Player, other *omp.Player, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if other == nil || !other.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetMarkerForPlayer(player.Handle(), other.Handle(), color)
	return res, nil
}

// SetMarkerRadius wraps CAPI Core_SetMarkerRadius
func (a *API) SetMarkerRadius(playerMarkerRadius float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_SetMarkerRadius(playerMarkerRadius)
	return res, nil
}

// SetModel wraps CAPI Pickup_SetModel
func (a *API) SetModel(pickup *omp.Pickup, model int32, update bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_SetModel(pickup.Handle(), model, update)
	return res, nil
}

// SetName wraps CAPI Player_SetName
func (a *API) SetName(player *omp.Player, name string) (int32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return 0, ErrInvalidPlayer
	}
	res := a.capi.Player_SetName(player.Handle(), name)
	return res, nil
}

// SetNameTagsDrawDistance wraps CAPI Core_SetNameTagsDrawDistance
func (a *API) SetNameTagsDrawDistance(distance float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_SetNameTagsDrawDistance(distance)
	return res, nil
}

// SetNumberPlate wraps CAPI Vehicle_SetNumberPlate
func (a *API) SetNumberPlate(vehicle *omp.Vehicle, numberPlate string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetNumberPlate(vehicle.Handle(), numberPlate)
	return res, nil
}

// SetObjectMaterial wraps CAPI Object_SetMaterial
func (a *API) SetObjectMaterial(object *omp.Object, materialIndex int32, modelID int32, textureLibrary string, textureName string, materialColor uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_SetMaterial(object.Handle(), materialIndex, modelID, textureLibrary, textureName, materialColor)
	return res, nil
}

// SetObjectMaterialText wraps CAPI Object_SetMaterialText
func (a *API) SetObjectMaterialText(object *omp.Object, text string, materialIndex int32, materialSize int32, fontface string, fontsize int32, bold bool, fontColor uint32, backgroundColor uint32, textalignment int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_SetMaterialText(object.Handle(), text, materialIndex, materialSize, fontface, fontsize, bold, fontColor, backgroundColor, textalignment)
	return res, nil
}

// SetObjectNoCameraCollision wraps CAPI Object_SetNoCameraCollision
func (a *API) SetObjectNoCameraCollision(object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_SetNoCameraCollision(object.Handle())
	return res, nil
}

// SetObjectPos wraps CAPI Object_SetPos
func (a *API) SetObjectPos(object *omp.Object, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_SetPos(object.Handle(), x, y, z)
	return res, nil
}

// SetObjectRot wraps CAPI Object_SetRot
func (a *API) SetObjectRot(object *omp.Object, rotationX float32, rotationY float32, rotationZ float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_SetRot(object.Handle(), rotationX, rotationY, rotationZ)
	return res, nil
}

// SetParamsCarDoors wraps CAPI Vehicle_SetParamsCarDoors
func (a *API) SetParamsCarDoors(vehicle *omp.Vehicle, frontLeft int32, frontRight int32, rearLeft int32, rearRight int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetParamsCarDoors(vehicle.Handle(), frontLeft, frontRight, rearLeft, rearRight)
	return res, nil
}

// SetParamsCarWindows wraps CAPI Vehicle_SetParamsCarWindows
func (a *API) SetParamsCarWindows(vehicle *omp.Vehicle, frontLeft int32, frontRight int32, rearLeft int32, rearRight int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetParamsCarWindows(vehicle.Handle(), frontLeft, frontRight, rearLeft, rearRight)
	return res, nil
}

// SetParamsEx wraps CAPI Vehicle_SetParamsEx
func (a *API) SetParamsEx(vehicle *omp.Vehicle, engine int32, lights int32, alarm int32, doors int32, bonnet int32, boot int32, objective int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetParamsEx(vehicle.Handle(), engine, lights, alarm, doors, bonnet, boot, objective)
	return res, nil
}

// SetParamsForPlayer wraps CAPI Vehicle_SetParamsForPlayer
func (a *API) SetParamsForPlayer(vehicle *omp.Vehicle, player *omp.Player, objective int32, doors int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Vehicle_SetParamsForPlayer(vehicle.Handle(), player.Handle(), objective, doors)
	return res, nil
}

// SetParamsSirenState wraps CAPI Vehicle_SetParamsSirenState
func (a *API) SetParamsSirenState(vehicle *omp.Vehicle, sirenState bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetParamsSirenState(vehicle.Handle(), sirenState)
	return res, nil
}

// SetPickupPos wraps CAPI Pickup_SetPos
func (a *API) SetPickupPos(pickup *omp.Pickup, x float32, y float32, z float32, update bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_SetPos(pickup.Handle(), x, y, z, update)
	return res, nil
}

// SetPickupVirtualWorld wraps CAPI Pickup_SetVirtualWorld
func (a *API) SetPickupVirtualWorld(pickup *omp.Pickup, virtualworld int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_SetVirtualWorld(pickup.Handle(), virtualworld)
	return res, nil
}

// SetPlayerColor wraps CAPI Player_SetColor
func (a *API) SetPlayerColor(player *omp.Player, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetColor(player.Handle(), color)
	return res, nil
}

// SetPlayerFacingAngle wraps CAPI Player_SetFacingAngle
func (a *API) SetPlayerFacingAngle(player *omp.Player, angle float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetFacingAngle(player.Handle(), angle)
	return res, nil
}

// SetPlayerGravity wraps CAPI Player_SetGravity
func (a *API) SetPlayerGravity(player *omp.Player, gravity float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetGravity(player.Handle(), gravity)
	return res, nil
}

// SetPlayerHealth wraps CAPI Player_SetHealth
func (a *API) SetPlayerHealth(player *omp.Player, health float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetHealth(player.Handle(), health)
	return res, nil
}

// SetPlayerObjectMaterial wraps CAPI PlayerObject_SetMaterial
func (a *API) SetPlayerObjectMaterial(player *omp.Player, object *omp.PlayerObject, materialIndex int32, modelID int32, textureLibrary string, textureName string, materialColor uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_SetMaterial(player.Handle(), object.Handle(), materialIndex, modelID, textureLibrary, textureName, materialColor)
	return res, nil
}

// SetPlayerObjectMaterialText wraps CAPI PlayerObject_SetMaterialText
func (a *API) SetPlayerObjectMaterialText(player *omp.Player, object *omp.PlayerObject, text string, materialIndex int32, materialSize int32, fontface string, fontsize int32, bold bool, fontColor uint32, backgroundColor uint32, textalignment int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_SetMaterialText(player.Handle(), object.Handle(), text, materialIndex, materialSize, fontface, fontsize, bold, fontColor, backgroundColor, textalignment)
	return res, nil
}

// SetPlayerObjectNoCameraCollision wraps CAPI PlayerObject_SetNoCameraCollision
func (a *API) SetPlayerObjectNoCameraCollision(player *omp.Player, object *omp.PlayerObject) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_SetNoCameraCollision(player.Handle(), object.Handle())
	return res, nil
}

// SetPlayerObjectPos wraps CAPI PlayerObject_SetPos
func (a *API) SetPlayerObjectPos(player *omp.Player, object *omp.PlayerObject, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_SetPos(player.Handle(), object.Handle(), x, y, z)
	return res, nil
}

// SetPlayerObjectRot wraps CAPI PlayerObject_SetRot
func (a *API) SetPlayerObjectRot(player *omp.Player, object *omp.PlayerObject, rotationX float32, rotationY float32, rotationZ float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_SetRot(player.Handle(), object.Handle(), rotationX, rotationY, rotationZ)
	return res, nil
}

// SetPlayerPos wraps CAPI Player_SetPos
func (a *API) SetPlayerPos(player *omp.Player, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetPos(player.Handle(), x, y, z)
	return res, nil
}

// SetPlayerSkin wraps CAPI Player_SetSkin
func (a *API) SetPlayerSkin(player *omp.Player, skin int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetSkin(player.Handle(), skin)
	return res, nil
}

// SetPlayerSpawnInfo wraps CAPI Player_SetSpawnInfo
func (a *API) SetPlayerSpawnInfo(player *omp.Player, team uint8, skin int32, x float32, y float32, z float32, angle float32, weapon1 uint8, ammo1 uint32, weapon2 uint8, ammo2 uint32, weapon3 uint8, ammo3 uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetSpawnInfo(player.Handle(), team, skin, x, y, z, angle, weapon1, ammo1, weapon2, ammo2, weapon3, ammo3)
	return res, nil
}

// SetPlayerTextDrawAlignment wraps CAPI PlayerTextDraw_SetAlignment
func (a *API) SetPlayerTextDrawAlignment(player *omp.Player, textdraw *omp.PlayerTextDraw, alignment int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetAlignment(player.Handle(), textdraw.Handle(), alignment)
	return res, nil
}

// SetPlayerTextDrawBackgroundColor wraps CAPI PlayerTextDraw_SetBackgroundColor
func (a *API) SetPlayerTextDrawBackgroundColor(player *omp.Player, textdraw *omp.PlayerTextDraw, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetBackgroundColor(player.Handle(), textdraw.Handle(), color)
	return res, nil
}

// SetPlayerTextDrawBoxColor wraps CAPI PlayerTextDraw_SetBoxColor
func (a *API) SetPlayerTextDrawBoxColor(player *omp.Player, textdraw *omp.PlayerTextDraw, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetBoxColor(player.Handle(), textdraw.Handle(), color)
	return res, nil
}

// SetPlayerTextDrawColor wraps CAPI PlayerTextDraw_SetColor
func (a *API) SetPlayerTextDrawColor(player *omp.Player, textdraw *omp.PlayerTextDraw, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetColor(player.Handle(), textdraw.Handle(), color)
	return res, nil
}

// SetPlayerTextDrawFont wraps CAPI PlayerTextDraw_SetFont
func (a *API) SetPlayerTextDrawFont(player *omp.Player, textdraw *omp.PlayerTextDraw, font int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetFont(player.Handle(), textdraw.Handle(), font)
	return res, nil
}

// SetPlayerTextDrawLetterSize wraps CAPI PlayerTextDraw_SetLetterSize
func (a *API) SetPlayerTextDrawLetterSize(player *omp.Player, textdraw *omp.PlayerTextDraw, x float32, y float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetLetterSize(player.Handle(), textdraw.Handle(), x, y)
	return res, nil
}

// SetPlayerTextDrawOutline wraps CAPI PlayerTextDraw_SetOutline
func (a *API) SetPlayerTextDrawOutline(player *omp.Player, textdraw *omp.PlayerTextDraw, size int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetOutline(player.Handle(), textdraw.Handle(), size)
	return res, nil
}

// SetPlayerTextDrawPos wraps CAPI PlayerTextDraw_SetPos
func (a *API) SetPlayerTextDrawPos(player *omp.Player, textdraw *omp.PlayerTextDraw, x float32, y float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetPos(player.Handle(), textdraw.Handle(), x, y)
	return res, nil
}

// SetPlayerTextDrawPreviewModel wraps CAPI PlayerTextDraw_SetPreviewModel
func (a *API) SetPlayerTextDrawPreviewModel(player *omp.Player, textdraw *omp.PlayerTextDraw, model int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetPreviewModel(player.Handle(), textdraw.Handle(), model)
	return res, nil
}

// SetPlayerTextDrawPreviewRot wraps CAPI PlayerTextDraw_SetPreviewRot
func (a *API) SetPlayerTextDrawPreviewRot(player *omp.Player, textdraw *omp.PlayerTextDraw, rx float32, ry float32, rz float32, zoom float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetPreviewRot(player.Handle(), textdraw.Handle(), rx, ry, rz, zoom)
	return res, nil
}

// SetPlayerTextDrawPreviewVehCol wraps CAPI PlayerTextDraw_SetPreviewVehCol
func (a *API) SetPlayerTextDrawPreviewVehCol(player *omp.Player, textdraw *omp.PlayerTextDraw, color1 int32, color2 int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetPreviewVehCol(player.Handle(), textdraw.Handle(), color1, color2)
	return res, nil
}

// SetPlayerTextDrawProportional wraps CAPI PlayerTextDraw_SetProportional
func (a *API) SetPlayerTextDrawProportional(player *omp.Player, textdraw *omp.PlayerTextDraw, set bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetProportional(player.Handle(), textdraw.Handle(), set)
	return res, nil
}

// SetPlayerTextDrawSelectable wraps CAPI PlayerTextDraw_SetSelectable
func (a *API) SetPlayerTextDrawSelectable(player *omp.Player, textdraw *omp.PlayerTextDraw, set bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetSelectable(player.Handle(), textdraw.Handle(), set)
	return res, nil
}

// SetPlayerTextDrawShadow wraps CAPI PlayerTextDraw_SetShadow
func (a *API) SetPlayerTextDrawShadow(player *omp.Player, textdraw *omp.PlayerTextDraw, size int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetShadow(player.Handle(), textdraw.Handle(), size)
	return res, nil
}

// SetPlayerTextDrawString wraps CAPI PlayerTextDraw_SetString
func (a *API) SetPlayerTextDrawString(player *omp.Player, textdraw *omp.PlayerTextDraw, text string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetString(player.Handle(), textdraw.Handle(), text)
	return res, nil
}

// SetPlayerTextDrawTextSize wraps CAPI PlayerTextDraw_SetTextSize
func (a *API) SetPlayerTextDrawTextSize(player *omp.Player, textdraw *omp.PlayerTextDraw, x float32, y float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_SetTextSize(player.Handle(), textdraw.Handle(), x, y)
	return res, nil
}

// SetPlayerTextLabelDrawDistance wraps CAPI PlayerTextLabel_SetDrawDistance
func (a *API) SetPlayerTextLabelDrawDistance(player *omp.Player, textlabel *omp.PlayerTextLabel, distance float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_SetDrawDistance(player.Handle(), textlabel.Handle(), distance)
	return res, nil
}

// SetPlayerTextLabelLOS wraps CAPI PlayerTextLabel_SetLOS
func (a *API) SetPlayerTextLabelLOS(player *omp.Player, textlabel *omp.PlayerTextLabel, status bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_SetLOS(player.Handle(), textlabel.Handle(), status)
	return res, nil
}

// SetPlayerVelocity wraps CAPI Player_SetVelocity
func (a *API) SetPlayerVelocity(player *omp.Player, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetVelocity(player.Handle(), x, y, z)
	return res, nil
}

// SetPlayerVirtualWorld wraps CAPI Player_SetVirtualWorld
func (a *API) SetPlayerVirtualWorld(player *omp.Player, vw int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetVirtualWorld(player.Handle(), vw)
	return res, nil
}

// SetPlayerWeather wraps CAPI Player_SetWeather
func (a *API) SetPlayerWeather(player *omp.Player, weather int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetWeather(player.Handle(), weather)
	return res, nil
}

// SetPosFindZ wraps CAPI Player_SetPosFindZ
func (a *API) SetPosFindZ(player *omp.Player, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetPosFindZ(player.Handle(), x, y, z)
	return res, nil
}

// SetRaceCheckpoint wraps CAPI RaceCheckpoint_Set
func (a *API) SetRaceCheckpoint(player *omp.Player, typ int32, x float32, y float32, z float32, nextX float32, nextY float32, nextZ float32, radius float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.RaceCheckpoint_Set(player.Handle(), typ, x, y, z, nextX, nextY, nextZ, radius)
	return res, nil
}

// SetRespawnDelay wraps CAPI Vehicle_SetRespawnDelay
func (a *API) SetRespawnDelay(vehicle *omp.Vehicle, respawnDelay int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetRespawnDelay(vehicle.Handle(), respawnDelay)
	return res, nil
}

// SetScore wraps CAPI Player_SetScore
func (a *API) SetScore(player *omp.Player, score int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetScore(player.Handle(), score)
	return res, nil
}

// SetShopName wraps CAPI Player_SetShopName
func (a *API) SetShopName(player *omp.Player, name string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetShopName(player.Handle(), name)
	return res, nil
}

// SetSkillLevel wraps CAPI Player_SetSkillLevel
func (a *API) SetSkillLevel(player *omp.Player, weapon uint8, level int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetSkillLevel(player.Handle(), weapon, level)
	return res, nil
}

// SetSpecialAction wraps CAPI Player_SetSpecialAction
func (a *API) SetSpecialAction(player *omp.Player, action uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetSpecialAction(player.Handle(), action)
	return res, nil
}

// SetStringForPlayer wraps CAPI TextDraw_SetStringForPlayer
func (a *API) SetStringForPlayer(textdraw *omp.TextDraw, player *omp.Player, text string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.TextDraw_SetStringForPlayer(textdraw.Handle(), player.Handle(), text)
	return res, nil
}

// SetTeam wraps CAPI Player_SetTeam
func (a *API) SetTeam(player *omp.Player, team int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetTeam(player.Handle(), team)
	return res, nil
}

// SetTextDrawAlignment wraps CAPI TextDraw_SetAlignment
func (a *API) SetTextDrawAlignment(textdraw *omp.TextDraw, alignment int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetAlignment(textdraw.Handle(), alignment)
	return res, nil
}

// SetTextDrawBackgroundColor wraps CAPI TextDraw_SetBackgroundColor
func (a *API) SetTextDrawBackgroundColor(textdraw *omp.TextDraw, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetBackgroundColor(textdraw.Handle(), color)
	return res, nil
}

// SetTextDrawBoxColor wraps CAPI TextDraw_SetBoxColor
func (a *API) SetTextDrawBoxColor(textdraw *omp.TextDraw, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetBoxColor(textdraw.Handle(), color)
	return res, nil
}

// SetTextDrawColor wraps CAPI TextDraw_SetColor
func (a *API) SetTextDrawColor(textdraw *omp.TextDraw, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetColor(textdraw.Handle(), color)
	return res, nil
}

// SetTextDrawFont wraps CAPI TextDraw_SetFont
func (a *API) SetTextDrawFont(textdraw *omp.TextDraw, font int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetFont(textdraw.Handle(), font)
	return res, nil
}

// SetTextDrawLetterSize wraps CAPI TextDraw_SetLetterSize
func (a *API) SetTextDrawLetterSize(textdraw *omp.TextDraw, sizeX float32, sizeY float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetLetterSize(textdraw.Handle(), sizeX, sizeY)
	return res, nil
}

// SetTextDrawOutline wraps CAPI TextDraw_SetOutline
func (a *API) SetTextDrawOutline(textdraw *omp.TextDraw, size int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetOutline(textdraw.Handle(), size)
	return res, nil
}

// SetTextDrawPos wraps CAPI TextDraw_SetPos
func (a *API) SetTextDrawPos(textdraw *omp.TextDraw, x float32, y float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetPos(textdraw.Handle(), x, y)
	return res, nil
}

// SetTextDrawPreviewModel wraps CAPI TextDraw_SetPreviewModel
func (a *API) SetTextDrawPreviewModel(textdraw *omp.TextDraw, model int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetPreviewModel(textdraw.Handle(), model)
	return res, nil
}

// SetTextDrawPreviewRot wraps CAPI TextDraw_SetPreviewRot
func (a *API) SetTextDrawPreviewRot(textdraw *omp.TextDraw, rotationX float32, rotationY float32, rotationZ float32, zoom float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetPreviewRot(textdraw.Handle(), rotationX, rotationY, rotationZ, zoom)
	return res, nil
}

// SetTextDrawPreviewVehCol wraps CAPI TextDraw_SetPreviewVehCol
func (a *API) SetTextDrawPreviewVehCol(textdraw *omp.TextDraw, color1 int32, color2 int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetPreviewVehCol(textdraw.Handle(), color1, color2)
	return res, nil
}

// SetTextDrawProportional wraps CAPI TextDraw_SetProportional
func (a *API) SetTextDrawProportional(textdraw *omp.TextDraw, set bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetProportional(textdraw.Handle(), set)
	return res, nil
}

// SetTextDrawSelectable wraps CAPI TextDraw_SetSelectable
func (a *API) SetTextDrawSelectable(textdraw *omp.TextDraw, set bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetSelectable(textdraw.Handle(), set)
	return res, nil
}

// SetTextDrawShadow wraps CAPI TextDraw_SetShadow
func (a *API) SetTextDrawShadow(textdraw *omp.TextDraw, size int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetShadow(textdraw.Handle(), size)
	return res, nil
}

// SetTextDrawString wraps CAPI TextDraw_SetString
func (a *API) SetTextDrawString(textdraw *omp.TextDraw, text string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetString(textdraw.Handle(), text)
	return res, nil
}

// SetTextDrawTextSize wraps CAPI TextDraw_SetTextSize
func (a *API) SetTextDrawTextSize(textdraw *omp.TextDraw, sizeX float32, sizeY float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetTextSize(textdraw.Handle(), sizeX, sizeY)
	return res, nil
}

// SetTextLabelDrawDistance wraps CAPI TextLabel_SetDrawDistance
func (a *API) SetTextLabelDrawDistance(textlabel *omp.TextLabel, distance float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_SetDrawDistance(textlabel.Handle(), distance)
	return res, nil
}

// SetTextLabelLOS wraps CAPI TextLabel_SetLOS
func (a *API) SetTextLabelLOS(textlabel *omp.TextLabel, status bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_SetLOS(textlabel.Handle(), status)
	return res, nil
}

// SetTextLabelVirtualWorld wraps CAPI TextLabel_SetVirtualWorld
func (a *API) SetTextLabelVirtualWorld(textlabel *omp.TextLabel, world int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_SetVirtualWorld(textlabel.Handle(), world)
	return res, nil
}

// SetTime wraps CAPI Player_SetTime
func (a *API) SetTime(player *omp.Player, hour int32, minute int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetTime(player.Handle(), hour, minute)
	return res, nil
}

// SetToRespawn wraps CAPI Vehicle_SetToRespawn
func (a *API) SetToRespawn(vehicle *omp.Vehicle) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetToRespawn(vehicle.Handle())
	return res, nil
}

// SetType wraps CAPI Pickup_SetType
func (a *API) SetType(pickup *omp.Pickup, typ int32, update bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_SetType(pickup.Handle(), typ, update)
	return res, nil
}

// SetUseBox wraps CAPI TextDraw_SetUseBox
func (a *API) SetUseBox(textdraw *omp.TextDraw, use bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_SetUseBox(textdraw.Handle(), use)
	return res, nil
}

// SetVehicleHealth wraps CAPI Vehicle_SetHealth
func (a *API) SetVehicleHealth(vehicle *omp.Vehicle, health float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetHealth(vehicle.Handle(), health)
	return res, nil
}

// SetVehiclePos wraps CAPI Vehicle_SetPos
func (a *API) SetVehiclePos(vehicle *omp.Vehicle, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetPos(vehicle.Handle(), x, y, z)
	return res, nil
}

// SetVehicleSpawnInfo wraps CAPI Vehicle_SetSpawnInfo
func (a *API) SetVehicleSpawnInfo(vehicle *omp.Vehicle, modelid int32, x float32, y float32, z float32, rotation float32, color1 int32, color2 int32, respawnTime int32, interior int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetSpawnInfo(vehicle.Handle(), modelid, x, y, z, rotation, color1, color2, respawnTime, interior)
	return res, nil
}

// SetVehicleVelocity wraps CAPI Vehicle_SetVelocity
func (a *API) SetVehicleVelocity(vehicle *omp.Vehicle, x float32, y float32, z float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetVelocity(vehicle.Handle(), x, y, z)
	return res, nil
}

// SetVehicleVirtualWorld wraps CAPI Vehicle_SetVirtualWorld
func (a *API) SetVehicleVirtualWorld(vehicle *omp.Vehicle, virtualWorld int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetVirtualWorld(vehicle.Handle(), virtualWorld)
	return res, nil
}

// SetWantedLevel wraps CAPI Player_SetWantedLevel
func (a *API) SetWantedLevel(player *omp.Player, level int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetWantedLevel(player.Handle(), level)
	return res, nil
}

// SetWorldBounds wraps CAPI Player_SetWorldBounds
func (a *API) SetWorldBounds(player *omp.Player, xMax float32, xMin float32, yMax float32, yMin float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SetWorldBounds(player.Handle(), xMax, xMin, yMax, yMin)
	return res, nil
}

// SetWorldTime wraps CAPI Core_SetWorldTime
func (a *API) SetWorldTime(hour int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_SetWorldTime(hour)
	return res, nil
}

// SetZAngle wraps CAPI Vehicle_SetZAngle
func (a *API) SetZAngle(vehicle *omp.Vehicle, angle float32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_SetZAngle(vehicle.Handle(), angle)
	return res, nil
}

// ShowDialog wraps CAPI Dialog_Show
func (a *API) ShowDialog(player *omp.Player, dialog int32, style int32, title string, body string, button1 string, button2 string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Dialog_Show(player.Handle(), dialog, style, title, body, button1, button2)
	return res, nil
}

// ShowGameText wraps CAPI Player_ShowGameText
func (a *API) ShowGameText(player *omp.Player, text string, time int32, style int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ShowGameText(player.Handle(), text, time, style)
	return res, nil
}

// ShowGameTextForAll wraps CAPI Core_ShowGameTextForAll
func (a *API) ShowGameTextForAll(msg string, time int32, style int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_ShowGameTextForAll(msg, time, style)
	return res, nil
}

// ShowGangZoneForAll wraps CAPI GangZone_ShowForAll
func (a *API) ShowGangZoneForAll(gangzone *omp.GangZone, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_ShowForAll(gangzone.Handle(), color)
	return res, nil
}

// ShowGangZoneForPlayer wraps CAPI GangZone_ShowForPlayer
func (a *API) ShowGangZoneForPlayer(player *omp.Player, gangzone *omp.GangZone, color uint32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_ShowForPlayer(player.Handle(), gangzone.Handle(), color)
	return res, nil
}

// ShowMenuForPlayer wraps CAPI Menu_ShowForPlayer
func (a *API) ShowMenuForPlayer(menu *omp.Menu, player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if menu == nil || !menu.Valid() {
		return false, ErrInvalidMenu
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Menu_ShowForPlayer(menu.Handle(), player.Handle())
	return res, nil
}

// ShowNameTagForPlayer wraps CAPI Player_ShowNameTagForPlayer
func (a *API) ShowNameTagForPlayer(player *omp.Player, other *omp.Player, enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if other == nil || !other.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ShowNameTagForPlayer(player.Handle(), other.Handle(), enable)
	return res, nil
}

// ShowNameTags wraps CAPI Core_ShowNameTags
func (a *API) ShowNameTags(show bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_ShowNameTags(show)
	return res, nil
}

// ShowPickupForPlayer wraps CAPI Pickup_ShowForPlayer
func (a *API) ShowPickupForPlayer(player *omp.Player, pickup *omp.Pickup) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if pickup == nil || !pickup.Valid() {
		return false, ErrInvalidPickup
	}
	res := a.capi.Pickup_ShowForPlayer(player.Handle(), pickup.Handle())
	return res, nil
}

// ShowPlayerMarkers wraps CAPI Core_ShowPlayerMarkers
func (a *API) ShowPlayerMarkers(mode int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_ShowPlayerMarkers(mode)
	return res, nil
}

// ShowPlayerTextDraw wraps CAPI PlayerTextDraw_Show
func (a *API) ShowPlayerTextDraw(player *omp.Player, textdraw *omp.PlayerTextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_Show(player.Handle(), textdraw.Handle())
	return res, nil
}

// ShowTextDrawForAll wraps CAPI TextDraw_ShowForAll
func (a *API) ShowTextDrawForAll(textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_ShowForAll(textdraw.Handle())
	return res, nil
}

// ShowTextDrawForPlayer wraps CAPI TextDraw_ShowForPlayer
func (a *API) ShowTextDrawForPlayer(player *omp.Player, textdraw *omp.TextDraw) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidTextDraw
	}
	res := a.capi.TextDraw_ShowForPlayer(player.Handle(), textdraw.Handle())
	return res, nil
}

// Spawn wraps CAPI Player_Spawn
func (a *API) Spawn(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_Spawn(player.Handle())
	return res, nil
}

// SpectatePlayer wraps CAPI Player_SpectatePlayer
func (a *API) SpectatePlayer(player *omp.Player, target *omp.Player, mode int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if target == nil || !target.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_SpectatePlayer(player.Handle(), target.Handle(), mode)
	return res, nil
}

// SpectateVehicle wraps CAPI Player_SpectateVehicle
func (a *API) SpectateVehicle(player *omp.Player, target *omp.Vehicle, mode int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if target == nil || !target.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Player_SpectateVehicle(player.Handle(), target.Handle(), mode)
	return res, nil
}

// StopAudioStream wraps CAPI Player_StopAudioStream
func (a *API) StopAudioStream(player *omp.Player) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_StopAudioStream(player.Handle())
	return res, nil
}

// StopFlashForAll wraps CAPI GangZone_StopFlashForAll
func (a *API) StopFlashForAll(gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_StopFlashForAll(gangzone.Handle())
	return res, nil
}

// StopFlashForPlayer wraps CAPI GangZone_StopFlashForPlayer
func (a *API) StopFlashForPlayer(player *omp.Player, gangzone *omp.GangZone) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_StopFlashForPlayer(player.Handle(), gangzone.Handle())
	return res, nil
}

// StopObject wraps CAPI Object_Stop
func (a *API) StopObject(object *omp.Object) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidObject
	}
	res := a.capi.Object_Stop(object.Handle())
	return res, nil
}

// StopPlayerObject wraps CAPI PlayerObject_Stop
func (a *API) StopPlayerObject(player *omp.Player, object *omp.PlayerObject) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if object == nil || !object.Valid() {
		return false, ErrInvalidPlayerObject
	}
	res := a.capi.PlayerObject_Stop(player.Handle(), object.Handle())
	return res, nil
}

// TextDrawFromID wraps CAPI TextDraw_FromID
func (a *API) TextDrawFromID(textdrawid int32) (*omp.TextDraw, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.TextDraw_FromID(textdrawid)
	return omp.NewTextDraw(res), nil
}

// TextLabelFromID wraps CAPI TextLabel_FromID
func (a *API) TextLabelFromID(textlabelid int32) (*omp.TextLabel, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.TextLabel_FromID(textlabelid)
	return omp.NewTextLabel(res), nil
}

// TickCount wraps CAPI Core_TickCount
func (a *API) TickCount() (uint32, error) {
	if !a.IsInitialised() {
		return 0, ErrNotInitialised
	}
	res := a.capi.Core_TickCount()
	return res, nil
}

// ToggleChatTextReplacement wraps CAPI Core_ToggleChatTextReplacement
func (a *API) ToggleChatTextReplacement(enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_ToggleChatTextReplacement(enable)
	return res, nil
}

// ToggleClock wraps CAPI Player_ToggleClock
func (a *API) ToggleClock(player *omp.Player, enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ToggleClock(player.Handle(), enable)
	return res, nil
}

// ToggleControllable wraps CAPI Player_ToggleControllable
func (a *API) ToggleControllable(player *omp.Player, enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ToggleControllable(player.Handle(), enable)
	return res, nil
}

// ToggleGhostMode wraps CAPI Player_ToggleGhostMode
func (a *API) ToggleGhostMode(player *omp.Player, toggle bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ToggleGhostMode(player.Handle(), toggle)
	return res, nil
}

// ToggleSirenEnabled wraps CAPI Vehicle_ToggleSirenEnabled
func (a *API) ToggleSirenEnabled(vehicle *omp.Vehicle, status bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_ToggleSirenEnabled(vehicle.Handle(), status)
	return res, nil
}

// ToggleSpectating wraps CAPI Player_ToggleSpectating
func (a *API) ToggleSpectating(player *omp.Player, enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ToggleSpectating(player.Handle(), enable)
	return res, nil
}

// ToggleWidescreen wraps CAPI Player_ToggleWidescreen
func (a *API) ToggleWidescreen(player *omp.Player, enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	res := a.capi.Player_ToggleWidescreen(player.Handle(), enable)
	return res, nil
}

// UnBlockIPAddress wraps CAPI Core_UnBlockIpAddress
func (a *API) UnBlockIPAddress(ipAddress string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_UnBlockIpAddress(ipAddress)
	return res, nil
}

// UpdateDamageStatus wraps CAPI Vehicle_UpdateDamageStatus
func (a *API) UpdateDamageStatus(vehicle *omp.Vehicle, panels int32, doors int32, lights int32, tires int32) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if vehicle == nil || !vehicle.Valid() {
		return false, ErrInvalidVehicle
	}
	res := a.capi.Vehicle_UpdateDamageStatus(vehicle.Handle(), panels, doors, lights, tires)
	return res, nil
}

// UpdatePlayerTextLabelText wraps CAPI PlayerTextLabel_UpdateText
func (a *API) UpdatePlayerTextLabelText(player *omp.Player, textlabel *omp.PlayerTextLabel, color uint32, text string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidPlayerTextLabel
	}
	res := a.capi.PlayerTextLabel_UpdateText(player.Handle(), textlabel.Handle(), color, text)
	return res, nil
}

// UpdateTextLabelText wraps CAPI TextLabel_UpdateText
func (a *API) UpdateTextLabelText(textlabel *omp.TextLabel, color uint32, text string) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if textlabel == nil || !textlabel.Valid() {
		return false, ErrInvalidTextLabel
	}
	res := a.capi.TextLabel_UpdateText(textlabel.Handle(), color, text)
	return res, nil
}

// UseBox wraps CAPI PlayerTextDraw_UseBox
func (a *API) UseBox(player *omp.Player, textdraw *omp.PlayerTextDraw, use bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if player == nil || !player.Valid() {
		return false, ErrInvalidPlayer
	}
	if textdraw == nil || !textdraw.Valid() {
		return false, ErrInvalidPlayerTextDraw
	}
	res := a.capi.PlayerTextDraw_UseBox(player.Handle(), textdraw.Handle(), use)
	return res, nil
}

// UseCheck wraps CAPI GangZone_UseCheck
func (a *API) UseCheck(gangzone *omp.GangZone, enable bool) (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	if gangzone == nil || !gangzone.Valid() {
		return false, ErrInvalidGangZone
	}
	res := a.capi.GangZone_UseCheck(gangzone.Handle(), enable)
	return res, nil
}

// UseManualEngineAndLights wraps CAPI Vehicle_UseManualEngineAndLights
func (a *API) UseManualEngineAndLights() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Vehicle_UseManualEngineAndLights()
	return res, nil
}

// UsePedAnims wraps CAPI Core_UsePedAnims
func (a *API) UsePedAnims() (bool, error) {
	if !a.IsInitialised() {
		return false, ErrNotInitialised
	}
	res := a.capi.Core_UsePedAnims()
	return res, nil
}

// VehicleFromID wraps CAPI Vehicle_FromID
func (a *API) VehicleFromID(vehicleid int32) (*omp.Vehicle, error) {
	if !a.IsInitialised() {
		return nil, ErrNotInitialised
	}
	res := a.capi.Vehicle_FromID(vehicleid)
	return omp.NewVehicle(res), nil
}
